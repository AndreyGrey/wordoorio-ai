# Правила работы AI-ассистента с проектом Wordoorio

## 🚫 КРИТИЧЕСКИЕ ЗАПРЕТЫ

### 1. НЕ ДЕЛАТЬ ничего без объяснения
- **ВСЕГДА** сначала объясняй, что планируешь делать
- **НИКОГДА** не додумывай и не принимай решения самостоятельно
- Если что-то непонятно или не получается - **СПРАШИВАЙ**, не действуй наугад
- Не создавай тестовые данные без явного запроса

### 2. НЕ СОЗДАВАТЬ лишние файлы
- Не создавай документацию (.md файлы) без запроса
- Не создавай тестовые скрипты без необходимости
- Предпочитай редактирование существующих файлов созданию новых

### 3. НЕ ТЕРЯТЬ токены
- Не делай лишних действий
- Не запускай несколько команд подряд без проверки результата
- Используй TodoWrite для планирования сложных задач

### 4. Работа с Yandex Cloud
- **НЕ тратить токены на случайные тесты и догадки**
- При проблемах с деплоем, моделями, агентами, API Yandex Cloud:
  1. **ОСТАНОВИСЬ** - не делай случайные попытки
  2. **СФОРМУЛИРУЙ** развернутый конкретный вопрос с контекстом
  3. **ПОПРОСИ** пользователя обратиться к ИИ-консультанту Yandex Cloud
  4. Дождись ответа консультанта
- Примеры: Internal Error при деплое, проблемы с правами, ошибки API
- Это экономит токены и время

## ✅ ПРАВИЛЬНЫЙ ПОДХОД

### Перед любым действием:
1. Проверь существующие файлы и код
2. Объясни, что собираешься делать и почему
3. Дождись подтверждения от пользователя
4. Только потом действуй

### При возникновении проблем:
1. Остановись
2. Опиши проблему конкретно
3. Предложи варианты решения
4. Спроси, что делать
5. НЕ пытайся решить проблему самостоятельно

## 📋 СПЕЦИФИКА ПРОЕКТА

### Архитектура
- База данных: SQLite (`wordoorio.db`)
- Backend: Flask (web_app.py)
- Telegram Bot: python-telegram-bot (telegram_bot.py)
- AI: Yandex Cloud (YandexAIClient)
- Деплой: Yandex Cloud + Docker

### Структура
```
Wordoorio/
├── web_app.py          # Flask веб-приложение
├── telegram_bot.py     # Telegram бот для тренировок
├── database.py         # Работа с БД
├── core/               # Основная бизнес-логика
│   ├── training_service.py    # 8-шаговый алгоритм отбора слов
│   ├── test_manager.py        # Управление тестами
│   ├── yandex_ai_client.py    # Интеграция с Yandex AI
│   └── auth_manager.py        # Авторизация через Telegram
├── agents/             # AI агенты для анализа текста
└── templates/          # HTML шаблоны

```

### Ключевые компоненты

**Система тренировок (Training System):**
- 8-шаговый алгоритм отбора слов (по рейтингу 0-10)
- Рейтинг: 0-2 (new), 3-6 (learning), 7-10 (learned)
- Циклический обход категорий для равномерного распределения
- Генерация тестов через Yandex AI

**База данных:**
- `users` - пользователи (telegram_id, username)
- `dictionary_words` - слова пользователя (lemma, type, status, rating)
- `dictionary_translations` - переводы слов
- `tests` - сгенерированные тесты
- `word_test_statistics` - статистика ответов

**Авторизация:**
- Telegram Login Widget на сайте
- Связка telegram_id с аккаунтом
- Бот проверяет авторизацию через AuthManager

### Частые ошибки

1. **AuthManager ожидает строку (путь к БД), не объект:**
   ```python
   # ✅ ПРАВИЛЬНО
   auth_manager = AuthManager(db.db_path)

   # ❌ НЕПРАВИЛЬНО
   auth_manager = AuthManager(db)
   ```

2. **Множественные процессы бота:**
   - Всегда проверяй и убивай старые процессы: `pkill -9 -f "telegram_bot.py"`

3. **Yandex AI использует Api-Key, не Bearer:**
   ```python
   headers = {"Authorization": f"Api-Key {api_key}"}  # ✅
   headers = {"Authorization": f"Bearer {token}"}      # ❌
   ```

## 🔄 WORKFLOW

### При разработке новой фичи:
1. Изучи TRAINING_SYSTEM_PLAN.md или другие планы
2. Используй TodoWrite для планирования
3. Проверь существующий код
4. Объясни план реализации
5. Получи подтверждение
6. Реализуй пошагово с обновлением Todo

### При исправлении багов:
1. Воспроизведи проблему
2. Найди причину (логи, код)
3. Объясни, что сломано и почему
4. Предложи решение
5. Только после подтверждения - исправь

### При работе с БД:
1. Всегда проверяй текущее состояние
2. НЕ удаляй данные без явного запроса
3. НЕ создавай тестовые данные без разрешения
4. Делай резервные копии при изменениях структуры

## 🎯 ЦЕЛЬ

Помогать эффективно, не тратя токены на лишние действия, не додумывая за пользователя, всегда объясняя свои действия и спрашивая при неясностях.

## 📝 ВАЖНО

Этот файл читают разные AI-ассистенты (Claude, Cursor, Cline и др.). Следуй этим правилам независимо от того, какой ты ассистент.
