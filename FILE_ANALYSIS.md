# 📁 ПОЛНЫЙ АНАЛИЗ ФАЙЛОВ ПРОЕКТА

**Дата:** 7 декабря 2025
**Цель:** Детальный анализ каждого файла с указанием назначения, связей и статуса

---

## 📂 PYTHON FILES (18 файлов)

### 🎯 ROOT LEVEL

#### `web_app.py` (533 строки)
```
Назначение:  Flask веб-сервер - главный entry point приложения
Зависимости: → database.py, core/analysis_service.py, agents/youtube_agent.py
Используется: ✅ Production (gunicorn запускает этот файл)
Статус:      ⚠️ Активный, но с проблемами

Маршруты:
  GET  /                     → index.html ✅
  GET  /main                 → index.html ✅
  GET  /experimental         → experimental.html ✅
  GET  /v3                   → v3.html ✅
  GET  /history              → history.html ✅
  GET  /my-highlights        → my_highlights.html ❌ НЕ СУЩЕСТВУЕТ
  GET  /youtube              → youtube.html ❌ НЕ СУЩЕСТВУЕТ
  POST /analyze              → Основной анализ (v1_basic) ✅
  POST /experimental/analyze → Experimental анализ (v2_dual ❌)
  POST /v3/analyze           → V3 анализ (v3_adaptive) ✅
  POST /youtube/analyze      → YouTube субтитры ❌ (нет шаблона)
  GET  /api/history          → История анализов ✅
  GET  /api/search           → Поиск по словам ✅
  GET  /api/stats            → Статистика ✅

ПРОБЛЕМЫ:
  1. Строка 186: маршрут /my-highlights → несуществующий шаблон
  2. Строка 485: маршрут /youtube → несуществующий шаблон
  3. Строка 207: experimental использует v2_dual (не существует)

РЕКОМЕНДАЦИИ:
  - Удалить строки 186-189 (my-highlights маршрут)
  - Удалить строки 485-488 (youtube маршрут) ИЛИ создать youtube.html
  - Изменить experimental на использование v1_basic
```

---

#### `database.py` (231 строка)
```
Назначение:  SQLite репозиторий для сохранения истории анализов
Зависимости: Нет (использует только sqlite3, json, datetime)
Используется: ← web_app.py (save_analysis, get_recent_analyses)
Статус:      ✅ Отличный код, чистая реализация

Таблицы:
  analyses (id, original_text, analysis_date, total_highlights,
            total_words, session_id, ip_address)
  highlights (id, analysis_id, highlight_word, context,
              context_translation, dictionary_meanings)

Индексы:
  idx_analysis_date ON analyses(analysis_date)
  idx_highlight_word ON highlights(highlight_word)
  idx_analysis_id ON highlights(analysis_id)

Методы:
  save_analysis()        → Сохранить результат анализа
  get_recent_analyses()  → Получить последние N анализов
  get_analysis_by_id()   → Получить полный анализ по ID
  search_by_word()       → Найти анализы по слову
  get_stats()            → Общая статистика

ОЦЕНКА: 10/10 - Идеальный repository pattern
ПРОБЛЕМЫ: Нет
```

---

#### `refresh_token.py` (198 строк)
```
Назначение:  Умное обновление Yandex IAM токенов
Зависимости: subprocess, requests, os, time
Используется: ✅ Вероятно в cron (нужно проверить)
Статус:      ✅ Лучший из трех token refresh скриптов

Особенности:
  - Проверяет валидность токена ПЕРЕД обновлением (экономия!)
  - Работает локально и на production сервере
  - Умное определение пути .env
  - Перезапускает сервис после обновления

Стратегия:
  1. Читает текущий токен из .env
  2. Проверяет валидность через GPT API
  3. Если действителен → пропускает обновление
  4. Если истек → генерирует новый через yc CLI
  5. Обновляет .env файл
  6. Перезапускает wordoorio service

ОЦЕНКА: 9/10 - Отличная реализация
РЕКОМЕНДАЦИЯ: Оставить, удалить другие token скрипты
```

---

#### `server_token_refresh.py` (104 строки)
```
Назначение:  Обновление токенов на сервере через Service Account
Зависимости: subprocess, os
Используется: ❓ Неясно, возможно устарел
Статус:      ⚠️ Дубликат функциональности

Особенности:
  - Использует Service Account ключ (sa-key.json)
  - Хардкоден путь /var/www/wordoorio/
  - Всегда обновляет токен (нет проверки валидности)
  - Перезаписывает весь .env файл

Проблемы:
  - Дублирует функциональность refresh_token.py
  - Нет проверки текущего токена
  - Менее умный чем refresh_token.py

РЕКОМЕНДАЦИЯ: Переместить в archive/ или удалить
```

---

#### `deploy_token.py` (94 строки)
```
Назначение:  Деплой токена с локальной машины на сервер через SSH
Зависимости: subprocess, paramiko, os
Используется: ❓ Неясно
Статус:      ⚠️ Устаревший подход

Особенности:
  - Генерирует токен локально
  - Деплоит через SSH (paramiko)
  - IP хардкоден: 158.160.126.200
  - SSH ключ: ~/.ssh/id_rsa

Проблемы:
  - Хардкод IP адреса
  - Зависимость от локальной yc CLI
  - Требует paramiko (лишняя зависимость)
  - Менее надежно чем серверный refresh

РЕКОМЕНДАЦИЯ: Переместить в archive/ или удалить
```

---

### 📦 CONTRACTS

#### `contracts/__init__.py` (пустой)
```
Назначение:  Python package marker
Статус:      ✅ Стандартный файл
```

---

#### `contracts/analysis_contracts.py` (227 строк)
```
Назначение:  Единые типы данных и интерфейсы для всей системы
Зависимости: dataclasses, typing, abc, enum
Используется: → Импортируется везде (web_app, services, prompts)
Статус:      ✅ Идеальная архитектура

Что внутри:

  ENUMS:
    PromptVersion   → V1_BASIC, V2_DUAL, V3_ENHANCED

  DATACLASSES:
    Highlight       → Модель хайлайта (highlight, context, translation, etc)
    AnalysisRequest → Запрос (text, page_id, user_session)
    AnalysisResult  → Результат (success, highlights, stats, error)
    PageConfig      → Конфигурация страницы
    PromptMetadata  → Метаданные промпта

  ABSTRACT INTERFACES:
    PromptStrategy(ABC)     → Интерфейс для промптов
    AIClient(ABC)           → Интерфейс для AI клиентов
    AnalysisRepository(ABC) → Интерфейс для БД
    DeduplicationService(ABC) → Интерфейс для дедупликации

  UTILITIES:
    create_error_result()   → Создать результат с ошибкой
    create_success_result() → Создать успешный результат

ОЦЕНКА: 10/10 - Образцовый contract-based design
ПРОБЛЕМЫ: Нет
```

---

### 🧠 CORE

#### `core/__init__.py` (пустой)
```
Назначение:  Python package marker
Статус:      ✅ Стандартный файл
```

---

#### `core/analysis_service.py` (205 строк)
```
Назначение:  Orchestration layer - координирует весь процесс анализа
Зависимости: → prompt_manager, deduplication_service, contracts
Используется: ← web_app.py (все /analyze маршруты)
Статус:      ⚠️ Активный, но с критической проблемой

Класс: AnalysisService

  Маппинг промптов:
    'main'         → v1_basic ✅
    'experimental' → v2_dual  ❌ НЕ СУЩЕСТВУЕТ!
    'v3'           → v3_adaptive ✅

  Workflow:
    1. Валидация запроса (через request.validate())
    2. Выбор промпта по page_id
    3. AI анализ через стратегию промпта
    4. Дедупликация результатов
    5. Сбор метрик производительности
    6. Возврат результата

Методы:
  analyze_text()          → Главный метод
  get_available_pages()   → Список страниц с промптами
  register_page()         → Регистрация новой страницы

КРИТИЧЕСКАЯ ПРОБЛЕМА:
  Строка 37: 'experimental': 'v2_dual' ← v2_dual не существует!

РЕКОМЕНДАЦИЯ:
  Изменить на 'experimental': 'v1_basic'
```

---

#### `core/yandex_ai_client.py` (673 строки)
```
Назначение:  Интеграция с Yandex Cloud AI сервисами
Зависимости: requests, json, os, dotenv
Используется: ← prompt strategies (v1_basic, v3_adaptive)
Статус:      ✅ Активный, хорошая реализация

Класс: YandexAIClient

API интеграции:
  1. Yandex GPT API       → Анализ лексики
  2. Yandex Translate API → Переводы текста
  3. Yandex Dictionary API → Словарные значения

Особенности:
  - PRIMITIVE_WORDS (173 строки) → Фильтр базовой лексики
  - Парсинг markdown из GPT ответов
  - Валидация наличия highlight в context
  - Таймауты для всех API запросов
  - Подсчет стоимости токенов

Публичные методы:
  analyze_linguistic_highlights() → Основной анализ
  request_gpt()                   → Запрос к GPT
  translate_text()                → Перевод текста
  get_dictionary_meanings()       → Словарные значения

Приватные методы:
  _create_highlights_prompt()     → Создание промпта
  _request_yandex_gpt()          → Запрос к API
  _parse_gpt_response()          → Парсинг ответа
  _get_yandex_dict_translations() → Переводы через словарь

ПРОБЛЕМЫ:
  1. Нет кэширования переводов (повторные API запросы)
  2. Логирование через print() вместо logging
  3. Парсинг markdown дублируется в prompt strategies

ОЦЕНКА: 8/10 - Хорошо, но нужен кэш
```

---

### 🎨 CORE/PROMPTS

#### `core/prompts/__init__.py` (пустой)
```
Назначение:  Python package marker
Статус:      ✅ Стандартный файл
```

---

#### `core/prompts/prompt_manager.py` (132 строки)
```
Назначение:  Центральное управление версиями промптов
Зависимости: → v1_basic, v2_dual ❌, v3_adaptive, contracts
Используется: ← analysis_service
Статус:      ❌ КРИТИЧЕСКАЯ ПРОБЛЕМА

Класс: PromptManager

Методы:
  register_prompt()       → Регистрация стратегии
  get_prompt()           → Получить по version_id
  list_versions()        → Метаданные всех версий
  get_stable_version()   → Текущая стабильная версия
  recommend_version()    → Рекомендация на основе требований

КРИТИЧЕСКАЯ ПРОБЛЕМА:
  Строка 25: from core.prompts.versions.v2_dual import DualPromptV2
  ❌ Файл v2_dual.py НЕ СУЩЕСТВУЕТ!

  Строка 29: self.register_prompt(DualPromptV2())
  ❌ Не может выполниться

  Строка 34: except ImportError as e:
  ⚠️ Ошибка обрабатывается, но пишется в лог

Последствия:
  - При каждом запуске ошибка в логах
  - v2_dual не регистрируется
  - При запросе 'v2_dual' будет ValueError

FIX:
  Удалить строки 25 и 29
  ИЛИ создать v2_dual.py

ОЦЕНКА: 4/10 - Хорошая идея, плохая реализация
```

---

#### `core/prompts/versions/__init__.py` (пустой)
```
Назначение:  Python package marker
Статус:      ✅ Стандартный файл
```

---

#### `core/prompts/versions/v1_basic.py` (221 строка)
```
Назначение:  Стабильный промпт для основной версии
Зависимости: json, re, contracts
Используется: ← prompt_manager (зарегистрирован как 'v1_basic')
Статус:      ✅ Активный, стабильный

Класс: BasicPromptV1(PromptStrategy)

Метаданные:
  id: v1_basic
  name: Базовая версия
  is_stable: True
  performance_score: 85.0
  estimated_cost: 0.4₽

Workflow:
  1. Создание промпта с инструкциями
  2. Запрос к GPT через ai_client
  3. Парсинг JSON ответа
  4. Валидация каждого хайлайта
  5. Добавление словарных значений
  6. Фильтрация дубликатов перевода

Валидация:
  - Минимум 6 слов в context
  - Только ОДНО предложение (не несколько)
  - highlight должен быть в context
  - Все required поля присутствуют

Промпт инструкции:
  БРАТЬ: выразительные слова, коллокации, идиомы, фразовые глаголы
  НЕ БРАТЬ: базовую лексику, техническую терминологию, списки

ОЦЕНКА: 9/10 - Отличный промпт, хорошая валидация
```

---

#### `core/prompts/versions/v3_adaptive.py` (585 строк)
```
Назначение:  Новый промпт с patterns и few-shot learning
Зависимости: json, re, contracts
Используется: ← prompt_manager (зарегистрирован как 'v3_adaptive')
Статус:      ✅ Активный, экспериментальный

Класс: AdaptivePromptV3(PromptStrategy)

Метаданные:
  id: v3_adaptive
  name: V3 Adaptive
  is_stable: False
  performance_score: 95.0
  estimated_cost: 0.6₽

Инновации:
  1. Few-shot примеры (✅ и ❌ примеры)
  2. Два типа находок: HIGHLIGHTS и PATTERNS
  3. Pattern templates с [placeholders]
  4. Финальная проверка в самом промпте

Типы находок:

  HIGHLIGHTS:
    - type: "word" или "expression"
    - Выразительная лексика для запоминания
    - Примеры: "compelling", "shed light on"

  PATTERNS:
    - type: "pattern"
    - Языковые конструкции
    - Примеры: "the way [subject] [verb] it"

Валидация:
  - highlight/example должны быть в context
  - Наличие [placeholders] в pattern_template
  - why_interesting не generic (фильтр флуфа)

ПРОБЛЕМА:
  Рядом лежит v3_adaptive.py.backup (15 KB) - УДАЛИТЬ!

ОЦЕНКА: 10/10 - Отличная инновация, лучший промпт
```

---

#### ❌ `core/prompts/versions/v2_dual.py`
```
Назначение:  Dual-prompt стратегия (слова отдельно, фразы отдельно)
Статус:      НЕ СУЩЕСТВУЕТ

Где упоминается:
  1. prompt_manager.py:25  → import
  2. prompt_manager.py:29  → register
  3. analysis_service.py:37 → mapping 'experimental'
  4. page_configs.py:247   → AnalysisMode.DETAILED
  5. page_configs.py:254   → AnalysisMode.PROFESSIONAL

Последствия:
  - ImportError при каждом запуске
  - При запросе /experimental будет crash
  - В логах постоянно ошибка

РЕШЕНИЕ:
  Вариант 1: Создать v2_dual.py (скопировать логику из v3)
  Вариант 2: Удалить все ссылки, использовать v1_basic
```

---

#### ❌ `core/prompts/versions/v3_adaptive.py.backup`
```
Назначение:  Backup файл
Размер:      15 KB
Статус:      МУСОР

РЕКОМЕНДАЦИЯ: rm core/prompts/versions/v3_adaptive.py.backup
```

---

### 🔧 CORE/SERVICES

#### `core/services/deduplication_service.py` (335 строк)
```
Назначение:  Интеллектуальное удаление дубликатов хайлайтов
Зависимости: difflib, re, dataclasses, enum, contracts
Используется: ← analysis_service (после AI анализа)
Статус:      ✅ Отличная реализация

Класс: DeduplicationService

Типы схожести:
  EXACT_DUPLICATE   → "walk" == "walk"
  MORPHOLOGICAL     → "walk" ≈ "walking" ≈ "walked"
  SEMANTIC          → "big" ≈ "large" ≈ "huge"
  PARTIAL_OVERLAP   → "make decision" ⊃ "decision making"

Алгоритмы:

  Морфологический:
    - Убирает окончания: ing, ed, s, er, est, ly
    - Сравнивает основы слов
    - Использует difflib.SequenceMatcher

  Семантический:
    - Предопределенные группы синонимов
    - Для фраз: пересечение ключевых слов
    - Коэффициент Жаккара

  Частичное перекрытие:
    - Проверка subset/superset
    - Бонус за порядок слов

Оптимизации:
  - Кэш для результатов сравнения
  - Удаляет хайлайт с меньшим importance_score
  - Статистика дубликатов

Методы:
  deduplicate_highlights()  → Основной метод
  analyze_duplications()    → Статистика дубликатов
  get_statistics()          → Метрики качества

ОЦЕНКА: 10/10 - Блестящая реализация, умные алгоритмы
ПРОБЛЕМЫ: Нет
```

---

### 🤖 AGENTS

#### `agents/youtube_agent.py` (309 строк)
```
Назначение:  Извлечение субтитров из YouTube видео
Зависимости: re, requests, json, subprocess, youtube-transcript-api
Используется: ← web_app.py маршрут /youtube/analyze
Статус:      ⚠️ Код есть, но шаблон youtube.html НЕ СУЩЕСТВУЕТ

Класс: YouTubeTranscriptAgent

Функциональность:
  1. Парсинг video_id из различных URL форматов
  2. Извлечение транскриптов (youtube-transcript-api)
  3. Получение названия видео (oEmbed API)
  4. Обработка ошибок (disabled subs, private)

Поддерживаемые форматы URL:
  - youtube.com/watch?v=VIDEO_ID
  - youtu.be/VIDEO_ID
  - youtube.com/embed/VIDEO_ID
  - Прямой video_id (11 символов)

Методы:
  extract_transcript()      → Основной метод
  get_video_title()        → Название через oEmbed
  extract_video_id()       → Парсинг ID из URL
  get_available_languages() → Список языков субтитров

Обработка ошибок:
  - TranscriptsDisabled → "Субтитры отключены"
  - VideoUnavailable   → "Видео недоступно"
  - RequestBlocked     → "Слишком много запросов"

ПРОБЛЕМА:
  web_app.py:485 → render_template('youtube.html')
  ❌ Файл youtube.html НЕ СУЩЕСТВУЕТ!

РЕШЕНИЕ:
  Вариант 1: Создать youtube.html
  Вариант 2: Удалить маршрут /youtube и этот файл

ОЦЕНКА: 8/10 - Хороший код, но неиспользуемый
```

---

### ❌ INTERFACE (МЕРТВАЯ ПАПКА)

#### `interface/pages/page_configs.py` (322 строки)
```
Назначение:  Централизованные конфигурации страниц
Зависимости: dataclasses, typing, enum
Используется: НИГДЕ! (проверено через grep)
Статус:      ❌ ПОЛНОСТЬЮ МЕРТВЫЙ КОД

Что внутри:

  ENUMS:
    PageType        → MAIN, ANALYSIS, RESULTS, SETTINGS
    AnalysisMode    → QUICK, DETAILED, PROFESSIONAL

  DATACLASSES:
    LoadingConfig   → Настройки анимации загрузки
    PromptConfig    → Настройки промптов
    UIConfig        → Настройки UI
    PageConfig      → Общая конфигурация

  КЛАСС:
    PageConfigManager → Управление конфигами

  МЕТОДЫ:
    get_config()              → Получить конфиг страницы
    update_config()           → Обновить конфиг
    get_analysis_mode_config() → Конфиг для режима
    export_config()           → Экспорт в dict

ПРОБЛЕМЫ:
  1. НИГДЕ НЕ ИМПОРТИРУЕТСЯ
  2. Ссылается на несуществующий v2_dual (строки 247, 254)
  3. Дублирует функциональность analysis_service
  4. 322 строки мертвого кода

ПРОВЕРКА:
  grep -r "page_configs" . --include="*.py" --exclude-dir=venv
  Результат: ПУСТО (кроме самого файла)

РЕКОМЕНДАЦИЯ:
  rm -rf interface/

  Экономия: 322 строки, 10 KB

ОЦЕНКА: 0/10 - Мертвый код, удалить полностью
```

---

## 🌐 TEMPLATES (4 файла)

#### `templates/index.html` (20,383 байт)
```
Назначение:  Главная страница приложения
Маршрут:     / и /main
Статус:      ✅ Активный

Особенности:
  - Современный дизайн с градиентами
  - Анимация загрузки со словами из текста
  - Highlight карточки с переводами
  - Встроенный SVG логотип
  - Responsive дизайн

Компоненты:
  - LoadingAnimation.js
  - HighlightCard.js
```

---

#### `templates/experimental.html` (22,644 байт)
```
Назначение:  Экспериментальная страница (dual-prompt)
Маршрут:     /experimental
Статус:      ⚠️ Активный, но использует несуществующий v2_dual

Особенности:
  - Табы: Слова / Фразы / Все вместе
  - Summary pill с статистикой
  - Поддержка YouTube транскриптов (localStorage)
  - Original text блок

ПРОБЛЕМА:
  Использует v2_dual промпт (не существует)

  При запросе /experimental:
    1. web_app.py вызывает analysis_service
    2. analysis_service пытается получить 'v2_dual'
    3. prompt_manager выбрасывает ValueError
    4. 500 Internal Server Error

РЕШЕНИЕ:
  Изменить маппинг в analysis_service.py:
  'experimental': 'v1_basic'
```

---

#### `templates/v3.html` (29,540 байт)
```
Назначение:  V3 страница с patterns
Маршрут:     /v3
Статус:      ✅ Активный

Особенности:
  - Поддержка patterns (конструкций)
  - PatternCard компонент
  - Отдельное отображение highlights и patterns
  - Advanced UI

Компоненты:
  - LoadingAnimation.js
  - HighlightCard.js
  - PatternCard.js
```

---

#### `templates/history.html` (25,274 байт)
```
Назначение:  История анализов
Маршрут:     /history
Статус:      ✅ Активный

Особенности:
  - Список последних анализов
  - Поиск по словам
  - Статистика использования
  - Детальный просмотр анализа
```

---

#### ❌ `templates/youtube.html`
```
Назначение:  YouTube URL input форма
Статус:      НЕ СУЩЕСТВУЕТ

Запрашивается в:
  web_app.py:485 → @app.route('/youtube')

Последствие:
  500 Internal Server Error при заходе на /youtube

РЕШЕНИЕ:
  Создать файл ИЛИ удалить маршрут
```

---

#### ❌ `templates/my_highlights.html`
```
Назначение:  Сохраненные хайлайты пользователя
Статус:      НЕ СУЩЕСТВУЕТ

Запрашивается в:
  web_app.py:186 → @app.route('/my-highlights')

Последствие:
  500 Internal Server Error при заходе на /my-highlights

РЕШЕНИЕ:
  Удалить маршрут (функционал не реализован)
```

---

## 📦 STATIC FILES

#### `static/components/HighlightCard.js` (9,752 байт)
```
Назначение:  React-like компонент для карточки слова
Используется: ← index.html, experimental.html, v3.html
Статус:      ✅ Активный

Особенности:
  - Отображение highlight с переводом
  - Dictionary meanings (альтернативные значения)
  - Context предложение
  - Адаптивный дизайн
```

---

#### `static/components/LoadingAnimation.js` (27,651 байт)
```
Назначение:  Анимация загрузки со словами из текста
Используется: ← index.html, experimental.html, v3.html
Статус:      ✅ Активный

Особенности:
  - Извлекает слова из пользовательского текста
  - Показывает слова поочередно
  - Прогресс-бар
  - Fallback слова если текст короткий
```

---

#### `static/components/PatternCard.js` (3,011 байт)
```
Назначение:  Компонент для отображения patterns (v3)
Используется: ← v3.html
Статус:      ✅ Активный

Особенности:
  - Отображение pattern template
  - Example из текста
  - Why interesting объяснение
```

---

#### `static/components/PatternCard.css` (1,122 байт)
```
Назначение:  Стили для PatternCard
Используется: ← v3.html (импортируется в PatternCard.js)
Статус:      ✅ Активный
```

---

#### `static/images/wordoorio-logo.svg`
```
Назначение:  Логотип приложения
Статус:      ✅ Используется (но также встроен в HTML)
```

---

#### `static/favicon.ico` и `static/favicon.svg`
```
Назначение:  Иконки сайта
Статус:      ✅ Активные
```

---

## 🗑️ ФАЙЛЫ К УДАЛЕНИЮ

### КРИТИЧНЫЕ (удалить немедленно)

```bash
# Мертвый код
rm -rf interface/

# Backup файлы
rm core/prompts/versions/v3_adaptive.py.backup
```

### ДУБЛИКАТЫ (архивировать)

```bash
mkdir archive/

# Token refresh скрипты (оставить только refresh_token.py)
mv server_token_refresh.py archive/
mv deploy_token.py archive/
```

---

## 📊 СТАТИСТИКА

```
PYTHON FILES:     18 файлов
├─ Активные:      12 (67%)
├─ Устаревшие:    2 (11%)
├─ Мертвый код:   1 (6%)
└─ Несуществующие: 3 (17%)

TEMPLATES:        4 файла
├─ Активные:      4 (100%)
└─ Несуществующие: 2 (ссылки без файлов)

STATIC:           7 файлов
└─ Активные:      7 (100%)

ПРОБЛЕМЫ:
├─ ImportError:   1 (v2_dual)
├─ 404 templates: 2 (youtube, my_highlights)
└─ Мертвый код:   322 строки (page_configs)
```

---

## ✅ ЗАКЛЮЧЕНИЕ

### Качество кода: 8/10

**Отличные файлы:**
- contracts/analysis_contracts.py (10/10)
- core/services/deduplication_service.py (10/10)
- core/prompts/versions/v3_adaptive.py (10/10)
- database.py (10/10)

**Проблемные файлы:**
- core/prompts/prompt_manager.py (4/10 - ImportError)
- web_app.py (7/10 - несуществующие маршруты)
- interface/pages/page_configs.py (0/10 - мертвый код)

**Рекомендации:**
1. Удалить interface/ и backup файлы
2. Исправить ImportError в prompt_manager
3. Удалить маршруты к несуществующим шаблонам
4. Консолидировать token refresh скрипты
