# 📦 Полное руководство по requirements.txt

**Дата:** 2025-12-04
**Для:** Начинающих Python разработчиков
**Уровень:** Beginner-Friendly

---

## 📋 Что такое `requirements.txt`?

### Простыми словами:
`requirements.txt` — это **список покупок** для твоего Python проекта. Представь, что ты готовишь борщ и пишешь список продуктов на бумажке: картошка, свекла, капуста, морковь. Точно так же `requirements.txt` содержит список всех библиотек (packages), которые нужны твоему проекту для работы.

---

## 🤔 Зачем это нужно?

### Проблема без `requirements.txt`:

Представь ситуацию:
1. Ты написал код на своем компьютере
2. Установил библиотеки: `pip install flask`, `pip install requests`
3. Все работает! 🎉
4. Через месяц ты хочешь запустить проект на сервере...
5. **Проблема:** Ты забыл, какие библиотеки устанавливал! 😱

Или еще хуже:
- Твой коллега клонирует проект
- Запускает: `python web_app.py`
- **Ошибка:** `ModuleNotFoundError: No module named 'flask'`
- Он не знает, что нужно установить!

### Решение с `requirements.txt`:

```bash
# Человек клонирует проект
git clone https://github.com/yourproject

# Видит файл requirements.txt
cat requirements.txt
# Flask==2.3.3
# requests==2.31.0
# ...

# Одна команда устанавливает ВСЕ зависимости
pip install -r requirements.txt

# Все работает! ✅
python web_app.py
```

---

## 📋 Что внутри `requirements.txt`?

Давай посмотрим на наш файл:

```txt
Flask==2.3.3
python-dotenv==1.0.0
requests==2.31.0
Werkzeug==3.1.3
gunicorn==21.2.0
youtube-transcript-api==0.6.1
```

### Разберем каждую строку:

#### 1. `Flask==2.3.3`
- **Что это?** Веб-фреймворк для создания веб-приложений
- **Зачем нам?** Наш `web_app.py` это Flask приложение
- **Что делает?** Обрабатывает HTTP запросы, роутинг (`@app.route('/youtube')`), рендерит HTML templates

**Аналогия:** Flask — это как каркас дома. Без него ты не можешь построить веб-приложение.

---

#### 2. `python-dotenv==1.0.0`
- **Что это?** Библиотека для загрузки переменных из `.env` файла
- **Зачем нам?** Хранить секретные ключи (API токены) безопасно
- **Что делает?** Читает `.env` файл и загружает `YANDEX_IAM_TOKEN`, `YANDEX_FOLDER_ID` в переменные окружения

**Код в проекте:**
```python
from dotenv import load_dotenv
load_dotenv()  # ← Эта библиотека загружает .env
```

**Аналогия:** Сейф, где хранятся пароли и ключи. Ты не хочешь хранить их прямо в коде!

---

#### 3. `requests==2.31.0`
- **Что это?** Библиотека для HTTP запросов
- **Зачем нам?** Делать запросы к YouTube API, Yandex GPT, словарям
- **Что делает?** `requests.get(url)`, `requests.post(url, json=data)`

**Код в проекте:**
```python
# youtube_agent.py
response = requests.get(url, timeout=5)  # ← Эта библиотека
data = response.json()
```

**Аналогия:** Почтальон, который отправляет письма (HTTP запросы) на другие серверы.

---

#### 4. `Werkzeug==3.1.3`
- **Что это?** WSGI утилита (используется Flask под капотом)
- **Зачем нам?** Flask зависит от неё для обработки запросов
- **Что делает?** Низкоуровневая работа с HTTP протоколом

**Важно:** Мы её не импортируем напрямую, но Flask без неё не работает!

**Аналогия:** Двигатель в машине. Ты не крутишь его руками, но без него машина не поедет.

---

#### 5. `gunicorn==21.2.0`
- **Что это?** Production WSGI HTTP сервер
- **Зачем нам?** Для запуска Flask в production (на боевом сервере)
- **Что делает?** Обрабатывает множество запросов одновременно, стабильно работает

**Использование:**
```bash
# Development (на компьютере)
python web_app.py

# Production (на сервере)
gunicorn web_app:app
```

**Аналогия:** Разница между домашней плитой (development) и промышленной кухней ресторана (production).

---

#### 6. `youtube-transcript-api==0.6.1` ⭐ NEW!
- **Что это?** Библиотека для извлечения субтитров из YouTube видео
- **Зачем нам?** Наша новая YouTube функция!
- **Что делает?** Скачивает транскрипты без необходимости скачивать само видео

**Код в проекте:**
```python
from youtube_transcript_api import YouTubeTranscriptApi

agent = YouTubeTranscriptApi()
transcript = agent.fetch(video_id, languages=['en'])
```

**Аналогия:** Специальный инструмент, который умеет "вытаскивать" текст из YouTube видео.

**Почему мы её добавили?**
Во время аудита я обнаружил, что эта библиотека используется в `agents/youtube_agent.py`, но **не была указана** в `requirements.txt`! Это критическая ошибка — на production сервере YouTube функция просто не запустится без неё.

---

## 🔢 Что означают версии? (==2.3.3)

### Формат: `PackageName==Major.Minor.Patch`

**Пример:** `Flask==2.3.3`
- **2** — Major version (большие изменения, может сломать код)
- **3** — Minor version (новые фичи, обратно совместимые)
- **3** — Patch version (исправления багов)

### Зачем фиксировать версию?

**Без фиксации:**
```txt
Flask  # ← Любая версия
```

**Проблема:**
- Сегодня установится `Flask 2.3.3` — работает
- Через год установится `Flask 3.0.0` — сломается (изменился API)

**С фиксацией:**
```txt
Flask==2.3.3  # ← Всегда эта версия
```

**Результат:** Проект работает стабильно всегда, на любом сервере.

---

## 🛠️ Как работать с `requirements.txt`?

### 1. Установить все зависимости:
```bash
pip install -r requirements.txt
```
Это команда говорит pip: "Прочитай файл requirements.txt и установи все библиотеки из него"

### 2. Создать `requirements.txt` для своего проекта:
```bash
# После того как установил все библиотеки
pip freeze > requirements.txt
```
Команда `pip freeze` показывает все установленные библиотеки с версиями.

### 3. Добавить новую библиотеку:
```bash
# Шаг 1: Установить
pip install youtube-transcript-api

# Шаг 2: Добавить в requirements.txt
echo "youtube-transcript-api==0.6.1" >> requirements.txt
```

---

## 🎓 Почему это важно для нашего проекта?

### Сценарий без `youtube-transcript-api` в requirements.txt:

1. **На локальном компьютере (твой Mac/PC):**
   - Ты установил: `pip install youtube-transcript-api`
   - YouTube функция работает ✅

2. **На production сервере (158.160.126.200):**
   ```bash
   cd wordoorio
   pip install -r requirements.txt
   # ← youtube-transcript-api НЕ установится!

   python web_app.py
   # Запускается...

   # Пользователь открывает /youtube
   # Вставляет URL, нажимает GO!

   # ОШИБКА: ModuleNotFoundError: No module named 'youtube_transcript_api'
   # ❌ YouTube функция не работает!
   ```

3. **Решение — добавить в requirements.txt:**
   ```txt
   youtube-transcript-api==0.6.1
   ```

   Теперь на сервере:
   ```bash
   pip install -r requirements.txt
   # ✅ youtube-transcript-api установится автоматически
   ```

---

## 📊 Сравнение: До и После аудита

### До моего аудита:
```txt
Flask==2.3.3
python-dotenv==1.0.0
requests==2.31.0
Werkzeug==3.1.3
gunicorn==21.2.0
# ← youtube-transcript-api ОТСУТСТВУЕТ!
```

**Проблема:** YouTube функция работает локально, но сломается на production!

### После аудита:
```txt
Flask==2.3.3
python-dotenv==1.0.0
requests==2.31.0
Werkzeug==3.1.3
gunicorn==21.2.0
youtube-transcript-api==0.6.1  ← ДОБАВЛЕНО ✅
```

**Результат:** YouTube функция работает везде!

---

## 🎯 Key Takeaways (Главные выводы):

1. **`requirements.txt`** — список всех библиотек, нужных проекту
2. **Версии фиксируются** (`==2.3.3`) для стабильности
3. **Одна команда** (`pip install -r requirements.txt`) устанавливает все
4. **Критично для deployment** — без правильного requirements.txt проект не запустится на сервере
5. **Во время аудита** я нашел отсутствующую зависимость `youtube-transcript-api` и добавил её

---

## 💡 Лайфхак для разработчиков:

**Всегда проверяй requirements.txt перед деплоем:**
```bash
# 1. Посмотри какие импорты в коде
grep -r "^import\|^from" *.py

# 2. Сравни с requirements.txt
cat requirements.txt

# 3. Если чего-то не хватает — добавь!
```

Именно это я и сделал во время senior code review! 🎓

---

## 🔍 Real-World Example из нашего проекта

### Найденная проблема:

**Файл:** `agents/youtube_agent.py`
```python
from youtube_transcript_api import YouTubeTranscriptApi  # ← Используется!
```

**Файл:** `requirements.txt`
```txt
Flask==2.3.3
python-dotenv==1.0.0
requests==2.31.0
Werkzeug==3.1.3
gunicorn==21.2.0
# youtube-transcript-api ← ОТСУТСТВУЕТ!
```

### Как я это обнаружил:

```bash
# 1. Grep всех импортов
grep -r "from youtube_transcript_api" agents/

# Результат:
# agents/youtube_agent.py:from youtube_transcript_api import YouTubeTranscriptApi

# 2. Проверка requirements.txt
cat requirements.txt | grep youtube

# Результат: (пусто) ← Проблема!

# 3. Добавление зависимости
echo "youtube-transcript-api==0.6.1" >> requirements.txt
```

### Результат:
✅ Теперь при деплое на production библиотека установится автоматически!

---

## 📚 Дополнительные материалы

### Полезные команды pip:

```bash
# Показать все установленные пакеты
pip list

# Показать информацию о пакете
pip show flask

# Обновить пакет
pip install --upgrade flask

# Удалить пакет
pip uninstall flask

# Установить конкретную версию
pip install flask==2.3.3
```

### Best Practices:

1. **Всегда используй virtual environment:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Mac/Linux
   venv\Scripts\activate     # Windows
   ```

2. **Разделяй dev и prod зависимости:**
   ```txt
   # requirements.txt (production)
   Flask==2.3.3

   # requirements-dev.txt (development)
   pytest==7.0.0
   black==22.0.0
   ```

3. **Регулярно обновляй зависимости:**
   ```bash
   pip list --outdated
   ```

4. **Используй pip-tools для управления:**
   ```bash
   pip install pip-tools
   pip-compile requirements.in
   ```

---

**Автор:** Senior AI Developer
**Проект:** Wordoorio AI
**Версия:** 1.0
**Последнее обновление:** 2025-12-04
