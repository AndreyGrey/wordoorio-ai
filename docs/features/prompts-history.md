# 📝 ИСТОРИЯ ПРОМПТОВ WORDOORIO AI

## ⚠️ ВАЖНО: НЕ УДАЛЯТЬ ЗАПИСИ!
**Этот файл служит для ведения истории всех опробованных промптов.**
- ❌ **НЕ удаляйте** существующие записи
- ❌ **НЕ изменяйте** старые промпты  
- ✅ **ТОЛЬКО добавляйте** новые версии в конец файла
- ✅ Указывайте дату, версию и причину изменений

---

## 📅 2024-11-24 - INITIAL PROMPTS

### VERSION 1.0 - WORDS PROMPT (CURRENT)
**Назначение:** Поиск отдельных выразительных слов  
**Статус:** ✅ Активен в production

```
Ты — эксперт по продвинутой английской лексике, которая делает речь выразительной, натуральной и стильной. Найди ВСЕ слова и выражения из текста, которые действительно стоят изучения.

Проанализируй этот английский текст:
"{text}"

БРАТЬ (приоритет):
- Выразительные, точные, "живые" слова, которые часто встречаются в качественном медиа-контенте.
- Сильные коллокации (например: "compelling argument", "sheer determination").
- Идиомы, метафоры, устойчивые выражения.
- Продвинутые фразовые глаголы.
- Профессиональные термины, если они широко используются (например: "leverage", "scalability", "breakthrough").

НЕ БРАТЬ:
- Узкую, сухую техническую терминологию, понятную только специалистам.
- Частотную базовую лексику (простые слова, которые все знают).
- Списки слов, перечисления через запятую.
- Длинные описания или фрагменты предложений.

ТРЕБОВАНИЯ:
- Бери максимум потенциально полезных выражений. Если сомневаешься — бери.
- "highlight" должен быть одним словом или короткой фразой.
- "context" — только ОДНО предложение из текста, которое ОБЯЗАТЕЛЬНО содержит выбранное слово/фразу. 
- ВАЖНО: слово/фраза из "highlight" должно точно присутствовать в "context".
- "context_translation" — это перевод ТОЛЬКО выбранного слова/выражения (кратко, без пояснений).

Формат ответа — только массив JSON:
[
  {
    "highlight": "слово или выражение",
    "context": "одно предложение из текста",
    "context_translation": "перевод слова/выражения"
  }
]

Перед тем как вернуть ответ:
Проверь, что базовые и узкотехнические слова исключены, а лучшие выразительные выражения включены.

Верни только массив JSON.
```

### VERSION 1.0 - PHRASES PROMPT (НОВЫЙ)
**Назначение:** Поиск словосочетаний и нативных оборотов  
**Статус:** 🧪 В разработке

```
Ты — эксперт по английскому языку. Найди интересные словосочетания и нативные обороты речи из текста — фразы, которые звучат естественно и профессионально.

Проанализируй этот английский текст:
"{text}"

БРАТЬ (только фразы 2+ слов):
- Естественные словосочетания, которые носители используют как единое целое
- Фразовые глаголы в контексте  
- Профессиональные термины и устойчивые сочетания
- Идиоматические обороты и связки
- Выразительные сочетания, характерные для качественного письма

НЕ БРАТЬ:
- Отдельные слова
- Случайные сочетания слов без устойчивости
- Простые предлогные фразы

Ищи фразы, которые изучающие английский захотели бы запомнить и использовать.

JSON формат: [{"highlight": "фраза", "context": "предложение", "context_translation": "перевод фразы"}]
```

---

## 📝 ПРИМЕЧАНИЯ К ВЕРСИЯМ

**V1.0 Words Prompt:**
- Хорошо работает для отдельных слов
- Логика БРАТЬ/НЕ БРАТЬ эффективна
- Проблема: иногда находит фразы вместо слов

**V1.0 Phrases Prompt:**
- Создан для решения проблемы с фразами
- Убраны ограничивающие примеры
- Фокус на естественность и профессиональность

---

---

## 📅 2024-11-24 - IMPLEMENTATION STATUS

### ✅ РЕАЛИЗОВАНО В КОДЕ:
- YandexAIClient обновлен для двух параллельных запросов
- Agent 2 возвращает структуру `{words: [...], phrases: [...]}`
- Web API обновлен для новой структуры ответа
- Обратная совместимость с БД сохранена

### 🧪 ТЕСТЫ ПРОЙДЕНЫ:
- Два запроса к Yandex GPT выполняются последовательно
- Новая структура API работает корректно
- Обработка ошибок функционирует

### 📋 СТАТУС ПРОМПТОВ:
- **Words Prompt (V1.0)**: ✅ Активен, без изменений
- **Phrases Prompt (V1.0)**: ✅ Добавлен в код, готов к тестированию

---

---

## 📅 2024-11-24 - HOTFIX PRODUCTION

### VERSION 1.1 - WORDS PROMPT (ИСПРАВЛЕНИЕ ГАЛЛЮЦИНАЦИЙ)
**Назначение:** Поиск отдельных выразительных слов  
**Статус:** ✅ Активен в production
**Изменения:** Добавлены строгие требования против галлюцинаций GPT

**Критические добавления:**
- "КРИТИЧЕСКИ ВАЖНО: используй только слова и фразы которые ТОЧНО есть в исходном тексте"
- "НЕ придумывай и НЕ добавляй слова которых нет в тексте"

### VERSION 1.1 - PHRASES PROMPT (ИСПРАВЛЕНИЕ ГАЛЛЮЦИНАЦИЙ)
**Назначение:** Поиск словосочетаний и нативных оборотов  
**Статус:** ✅ Активен в production
**Изменения:** Добавлены строгие требования против галлюцинаций GPT

**Критические добавления:**
- "КРИТИЧЕСКИ ВАЖНО: используй только фразы которые ТОЧНО есть в данном тексте"
- "НЕ придумывай и НЕ добавляй фразы которых нет в тексте"
- "ЛЮБЫЕ слова или фразы которых НЕТ в исходном тексте" в список НЕ БРАТЬ

### 🐛 ПРОБЛЕМА РЕШЕНА:
- **Симптом:** Worker падал с timeout, GPT возвращал хайлайты которых не было в тексте
- **Решение:** Ужесточены промпты + увеличен gunicorn timeout до 120 секунд
- **Результат:** Стабильная работа на продакшене

---

## 🔄 СЛЕДУЮЩИЕ ВЕРСИИ
*Добавлять новые промпты ниже с указанием даты и изменений...*

---

## 📅 2024-12-02 - НОВАЯ АРХИТЕКТУРА ПРОМПТОВ

### 🏗️ АРХИТЕКТУРНЫЕ ИЗМЕНЕНИЯ:
**Статус:** ✅ Реализовано в production

Создана новая архитектура версионирования промптов:

**Структура:**
```
core/prompts/
├── versions/
│   ├── v1_basic.py      # Стабильная версия (единый промпт)
│   └── v2_dual.py       # Экспериментальная (два промпта)
├── prompt_registry.py   # Реестр промптов
└── prompt_selector.py   # Селектор промптов
```

**Преимущества:**
- ✅ Легко A/B тестировать промпты
- ✅ Можно откатиться на старую версию
- ✅ Метрики производительности (performance_score, estimated_cost)
- ✅ Четкое разделение стабильных и экспериментальных версий

---

## 📅 2024-12-03 - УБРАНЫ ИСКУССТВЕННЫЕ ОГРАНИЧЕНИЯ

### VERSION 1.2 - V1_BASIC (UNLIMITED APPROACH)
**Назначение:** Единый промпт для поиска выразительной лексики
**Статус:** ✅ Активен в production
**Файл:** `core/prompts/versions/v1_basic.py`

**Проблема:**
- AI ограничивал себя жесткими лимитами (3-5, 5-10, 15-30 хайлайтов в зависимости от длины)
- Один и тот же текст давал разные результаты при повторном анализе
- Пропускались достойные слова из-за искусственных ограничений

**Решение:**
```python
# УДАЛЕНО - старая логика с лимитами:
word_count = len(text.split())
if word_count < 20:
    target_count = "3-5"
elif word_count < 50:
    target_count = "5-10"
# ...

# НОВЫЙ ПОДХОД - без лимитов:
prompt = self._create_prompt(text)  # Без параметра target_count
```

**Ключевые изменения в промпте:**
```
Твоя задача: найти ВСЕ слова и выражения из текста, которые действительно стоят изучения.

ВАЖНО: НЕ ограничивай себя количеством! Бери абсолютно ВСЕ выражения,
которые соответствуют критериям ниже. Если в тексте 5 достойных слов - бери 5.
Если 50 - бери все 50.
```

**Валидация (усилена):**
- ✅ Минимум 6 слов в контексте
- ✅ Только ОДНО предложение (regex проверка на количество `.!?`)
- ✅ Хайлайт должен присутствовать в контексте
- ✅ Детальное логирование отсеянных результатов

---

### VERSION 1.2 - V2_DUAL (UNLIMITED APPROACH)
**Назначение:** Два специализированных промпта (слова + фразы)
**Статус:** 🧪 Экспериментальный
**Файл:** `core/prompts/versions/v2_dual.py`

**Те же изменения применены к обоим промптам:**

**Words Prompt:**
```
Твоя задача: найти ВСЕ ОТДЕЛЬНЫЕ ВЫРАЗИТЕЛЬНЫЕ СЛОВА (не фразы!)
из текста, которые достойны изучения.

ВАЖНО: НЕ ограничивай себя количеством! Бери абсолютно ВСЕ слова,
которые соответствуют критериям ниже.
```

**Phrases Prompt:**
```
Твоя задача: найти ВСЕ ФРАЗЫ И ВЫРАЖЕНИЯ (2+ слова) из текста,
которые достойны изучения.

ВАЖНО: НЕ ограничивай себя количеством! Бери абсолютно ВСЕ фразы,
которые соответствуют критериям ниже.
```

**Дополнительная валидация для dual-prompt:**
- ✅ Words: проверка что нет пробелов (одно слово)
- ✅ Phrases: проверка что есть пробелы (2+ слов)

---

### 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ:

**Гипотеза:**
- Больше хайлайтов на плотных текстах (может быть 50-100+)
- Стабильные результаты при повторном анализе одного текста
- Полный охват всей достойной лексики

**Риски:**
- Может быть избыточное количество хайлайтов
- Увеличение стоимости API запросов
- Больше времени на обработку

**План тестирования:**
1. Сравнить количество хайлайтов до/после
2. Проверить стабильность результатов
3. Оценить качество (есть ли "мусор")
4. Измерить стоимость и время обработки

---

## 📅 2024-12-03 - ИСПРАВЛЕНИЕ КОНТЕКСТА

### HOTFIX 1.2.1 - КОНТЕКСТ ОДНИМ ПРЕДЛОЖЕНИЕМ
**Проблема:** AI возвращал контексты из нескольких предложений (целые абзацы)
**Решение:** Добавлена валидация по количеству sentence endings

**В промптах добавлено:**
```
КРИТИЧЕСКИ ВАЖНО:
- "context" должен быть ТОЛЬКО ОДНИМ ПОЛНЫМ предложением из текста (8-15 слов)
- НЕ используй несколько предложений! Только ОДНО предложение с точкой в конце!
```

**В валидации добавлено:**
```python
import re
sentence_endings = re.findall(r'[.!?]', item["context"])
if len(sentence_endings) > 1:
    print(f"⚠️ пропускаю - контекст содержит {len(sentence_endings)} предложений")
    return False
```

---

## 📅 2024-12-03 - ИСПРАВЛЕНИЕ ПЕРЕВОДОВ

### КРИТИЧЕСКАЯ ПРОБЛЕМА: Переводы не работали
**Симптомы:**
- В UI показывались английские слова вместо русских переводов
- Логи показывали `✅ Перевод 'quirky' -> 'quirky'` (оригинал возвращался без перевода)

**Корневые причины:**

1. **IAM токен истекал каждые 12 часов** → 403 Permission Denied от Yandex Translate API
2. **Скрипт автообновления токенов перезаписывал весь .env** → терялись YANDEX_DICT_API_KEY и другие переменные

**Решения:**

✅ **Исправлен скрипт `server_token_refresh.py`:**
```python
# СТАРАЯ ВЕРСИЯ (плохо):
def update_env_file(token):
    env_content = f"YANDEX_IAM_TOKEN={token}\nYANDEX_FOLDER_ID=...\n"
    with open(env_path, 'w') as f:
        f.write(env_content)  # ❌ ПЕРЕЗАПИСЫВАЕТ ВСЕ

# НОВАЯ ВЕРСИЯ (правильно):
def update_env_file(token):
    with open(env_path, 'r') as f:
        content = f.read()
    updated_content = re.sub(
        r'YANDEX_IAM_TOKEN=.*',
        f'YANDEX_IAM_TOKEN={token}',
        content
    )  # ✅ ЗАМЕНЯЕТ ТОЛЬКО ТОКЕН, СОХРАНЯЕТ ОСТАЛЬНОЕ
```

✅ **Добавлено детальное логирование:**
```python
print(f"🔄 [TRANSLATE] Переводим: '{definition}'")
print(f"🔄 [TRANSLATE] IAM token: {self.iam_token[:20]}...")
print(f"🔄 [TRANSLATE] Status: {response.status_code}")
print(f"✅ [TRANSLATE] Получен перевод: '{translation}'")
```

✅ **Cron настроен правильно:**
- Запускается каждые 3 часа: `0 */3 * * *`
- Логи в `/var/log/wordoorio-tokens.log`
- Автоматический перезапуск сервиса после обновления

**Статус:** ✅ Проблема полностью решена, автообновление токенов работает без потери других переменных

---
