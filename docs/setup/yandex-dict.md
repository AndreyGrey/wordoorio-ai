# 📚 Настройка Yandex Dictionary API

## Что изменилось

Мы переключились с **Free Dictionary API** на **Yandex Dictionary API** для получения альтернативных русских переводов слов.

### ❌ Старая схема (Free Dictionary):
```
Английское слово → Английское определение → Перевод определения на русский
"leverage" → "The action of a lever..." → "Действие рычага..."
```

### ✅ Новая схема (Yandex Dictionary):
```
Английское слово → Русские переводы напрямую
"leverage" → ["рычаг", "влияние", "кредитное плечо"]
```

## 🔑 Как получить API ключ

### Шаг 1: Регистрация
1. Перейдите на https://yandex.ru/dev/dictionary/
2. Нажмите **"Подключить API"** или **"Получить ключ"**
3. Войдите через Яндекс ID

### Шаг 2: Создание приложения
1. Создайте новое приложение
2. Выберите **"Yandex Dictionary API"**
3. Укажите название: например, "Wordoorio"

### Шаг 3: Получите ключ
1. После создания приложения вы получите **API ключ**
2. Скопируйте его

### Шаг 4: Добавьте в .env
```bash
YANDEX_DICT_API_KEY=ваш_ключ_здесь
```

## 📊 Лимиты

**Бесплатный тариф:**
- 10,000 запросов в день
- 10 запросов в секунду

Этого более чем достаточно для нашего проекта.

## 🧪 Тестирование

После добавления ключа, можно протестировать:

```bash
# Локально
python3 web_app.py
```

Затем отправьте текст на анализ и проверьте, что в `dictionary_meanings` теперь приходят русские переводы, а не английские определения.

## 🔍 Пример ответа

**Для слова "leverage":**
```json
{
  "highlight": "leverage",
  "context": "We need to leverage our resources...",
  "context_translation": "использовать преимущество",
  "dictionary_meanings": [
    "рычаг",
    "влияние",
    "кредитное плечо",
    "использовать"
  ]
}
```

**Для фразы "opt-out requests":**
```json
{
  "highlight": "opt-out requests",
  "context": "scan a massive database...",
  "context_translation": "запросы на отказ",
  "dictionary_meanings": [
    "отказ",
    "запрос",
    "обращение"
  ]
}
```

## ✅ Checklist

- [ ] Получить API ключ на https://yandex.ru/dev/dictionary/
- [ ] Добавить `YANDEX_DICT_API_KEY=...` в `.env`
- [ ] Перезапустить приложение
- [ ] Протестировать анализ текста
- [ ] Проверить что dictionary_meanings содержат русские переводы

## 🚀 Деплой на сервер

После локального тестирования:

1. Обновите `.env` на сервере:
```bash
ssh yc-user@158.160.126.200
nano /var/www/wordoorio/.env
# Добавьте строку: YANDEX_DICT_API_KEY=ваш_ключ
```

2. Задеплойте обновленный код:
```bash
scp core/yandex_ai_client.py yc-user@158.160.126.200:/tmp/
ssh yc-user@158.160.126.200 "sudo cp /tmp/yandex_ai_client.py /var/www/wordoorio/core/ && sudo systemctl restart wordoorio"
```
