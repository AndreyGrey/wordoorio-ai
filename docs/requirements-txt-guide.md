# 📦 Полное руководство по requirements.txt

**Дата:** 2025-12-04
**Для:** Начинающих Python разработчиков
**Уровень:** Beginner-Friendly

---

## 📋 Что такое `requirements.txt`?

### Простыми словами:
`requirements.txt` — это **список покупок** для твоего Python проекта. Представь, что ты готовишь борщ и пишешь список продуктов на бумажке: картошка, свекла, капуста, морковь. Точно так же `requirements.txt` содержит список всех библиотек (packages), которые нужны твоему проекту для работы.

---

## 🤔 Зачем это нужно?

### Проблема без `requirements.txt`:

Представь ситуацию:
1. Ты написал код на своем компьютере
2. Установил библиотеки: `pip install flask`, `pip install requests`
3. Все работает! 🎉
4. Через месяц ты хочешь запустить проект на сервере...
5. **Проблема:** Ты забыл, какие библиотеки устанавливал! 😱

Или еще хуже:
- Твой коллега клонирует проект
- Запускает: `python web_app.py`
- **Ошибка:** `ModuleNotFoundError: No module named 'flask'`
- Он не знает, что нужно установить!

### Решение с `requirements.txt`:

```bash
# Человек клонирует проект
git clone https://github.com/yourproject

# Видит файл requirements.txt
cat requirements.txt
# Flask==2.3.3
# requests==2.31.0
# ...

# Одна команда устанавливает ВСЕ зависимости
pip install -r requirements.txt

# Все работает! ✅
python web_app.py
```

---

## 📋 Что внутри `requirements.txt`?

Давай посмотрим на наш файл:

```txt
Flask==2.3.3
python-dotenv==1.0.0
requests==2.31.0
Werkzeug==3.1.3
gunicorn==21.2.0
youtube-transcript-api==0.6.1
```

### Разберем каждую строку:

#### 1. `Flask==2.3.3`
- **Что это?** Веб-фреймворк для создания веб-приложений
- **Зачем нам?** Наш `web_app.py` это Flask приложение
- **Что делает?** Обрабатывает HTTP запросы, роутинг (`@app.route('/youtube')`), рендерит HTML templates

**Аналогия:** Flask — это как каркас дома. Без него ты не можешь построить веб-приложение.

---

#### 2. `python-dotenv==1.0.0`
- **Что это?** Библиотека для загрузки переменных из `.env` файла
- **Зачем нам?** Хранить секретные ключи (API токены) безопасно
- **Что делает?** Читает `.env` файл и загружает `YANDEX_IAM_TOKEN`, `YANDEX_FOLDER_ID` в переменные окружения

**Код в проекте:**
```python
from dotenv import load_dotenv
load_dotenv()  # ← Эта библиотека загружает .env
```

**Аналогия:** Сейф, где хранятся пароли и ключи. Ты не хочешь хранить их прямо в коде!

---

#### 3. `requests==2.31.0`
- **Что это?** Библиотека для HTTP запросов
- **Зачем нам?** Делать запросы к YouTube API, Yandex GPT, словарям
- **Что делает?** `requests.get(url)`, `requests.post(url, json=data)`

**Код в проекте:**
```python
# youtube_agent.py
response = requests.get(url, timeout=5)  # ← Эта библиотека
data = response.json()
```

**Аналогия:** Почтальон, который отправляет письма (HTTP запросы) на другие серверы.

---

#### 4. `Werkzeug==3.1.3`
- **Что это?** WSGI утилита (используется Flask под капотом)
- **Зачем нам?** Flask зависит от неё для обработки запросов
- **Что делает?** Низкоуровневая работа с HTTP протоколом

**Важно:** Мы её не импортируем напрямую, но Flask без неё не работает!

**Аналогия:** Двигатель в машине. Ты не крутишь его руками, но без него машина не поедет.

---

#### 5. `gunicorn==21.2.0`
- **Что это?** Production WSGI HTTP сервер
- **Зачем нам?** Для запуска Flask в production (на боевом сервере)
- **Что делает?** Обрабатывает множество запросов одновременно, стабильно работает

**Использование:**
```bash
# Development (на компьютере)
python web_app.py

# Production (на сервере)
gunicorn web_app:app
```

**Аналогия:** Разница между домашней плитой (development) и промышленной кухней ресторана (production).


---

## 🔢 Что означают версии? (==2.3.3)

### Формат: `PackageName==Major.Minor.Patch`

**Пример:** `Flask==2.3.3`
- **2** — Major version (большие изменения, может сломать код)
- **3** — Minor version (новые фичи, обратно совместимые)
- **3** — Patch version (исправления багов)

### Зачем фиксировать версию?

**Без фиксации:**
```txt
Flask  # ← Любая версия
```

**Проблема:**
- Сегодня установится `Flask 2.3.3` — работает
- Через год установится `Flask 3.0.0` — сломается (изменился API)

**С фиксацией:**
```txt
Flask==2.3.3  # ← Всегда эта версия
```

**Результат:** Проект работает стабильно всегда, на любом сервере.


---

## 🎯 Key Takeaways (Главные выводы):

1. **`requirements.txt`** — список всех библиотек, нужных проекту
2. **Версии фиксируются** (`==2.3.3`) для стабильности
3. **Одна команда** (`pip install -r requirements.txt`) устанавливает все
4. **Критично для deployment** — без правильного requirements.txt проект не запустится на сервере
5. **Во время аудита** я нашел отсутствующую зависимость `youtube-transcript-api` и добавил её

---
