# 🎨 Профессиональный анализ фронтенда Wordoorio

**Дата:** 12 декабря 2024
**Аналитик:** Frontend Architecture Review
**Контекст:** Максимальная чистка фронтенда в рамках агентов рефакторинга

---

## 📊 Резюме анализа

### Вердикт: Архитектура чистая, но требует удаления legacy кода

**Что работает хорошо:**
- ✅ Компонентный подход (переиспользуемые JS компоненты)
- ✅ Разделение ответственности (Header, AnalysisForm, HighlightCard отдельно)
- ✅ Единообразный дизайн (цвета, отступы, радиусы)
- ✅ Адаптивность (responsive design для мобильных)
- ✅ Локальное хранилище (HighlightsStorage.js)

**Что нужно почистить:**
- ❌ 2 экспериментальные страницы (experimental.html, v3.html) - legacy код
- ❌ PatternCard компонент - использует удалённые поля из Highlight
- ❌ 2 route на бекенде для experimental/v3 анализа

**Итого удалить:**
- 2 HTML файла (~1200 строк)
- 1 JS компонент + 1 CSS файл (~150 строк)
- 2 backend route

---

## 🗂️ Инвентаризация фронтенда

### HTML Страницы (Templates)

| Файл | Route | Назначение | Статус | Причина |
|------|-------|------------|--------|---------|
| `index.html` | `/` | Главная страница анализа | ✅ **KEEP** | Основная рабочая страница |
| `my-highlights.html` | `/my-highlights` | Сохранённые хайлайты | ✅ **KEEP** | Функциональная страница |
| `history.html` | `/history` | История анализов | ✅ **KEEP** | Функциональная страница |
| `dictionary.html` | `/dictionary` | Словарь пользователя | ✅ **KEEP** | Функциональная страница |
| `experimental.html` | `/experimental` | Dual-prompt тестирование | ❌ **DELETE** | Legacy, дублирует index.html |
| `v3.html` | `/v3` (не существует) | V3 adaptive prompt тест | ❌ **DELETE** | Legacy, нет роута |

**Вердикт:** 4 рабочих страницы остаются, 2 экспериментальных удаляются.

---

### JS Компоненты

| Файл | Зависимости | Использование | Статус | Причина |
|------|-------------|---------------|--------|---------|
| `AnalysisForm.js` | - | index.html, experimental.html | ✅ **KEEP** | Универсальная форма анализа |
| `HighlightCard.js` | - | Все страницы с хайлайтами | ✅ **KEEP** | Основной компонент карточки слова/фразы |
| `PatternCard.js` | - | v3.html | ❌ **DELETE** | Использует `pattern_template`, `why_interesting` (удалённые поля) |
| `Header.js` | Auth.js | Все страницы | ✅ **KEEP** | Универсальная шапка |
| `DictionaryWordRow.js` | - | dictionary.html | ✅ **KEEP** | Компонент строки словаря |
| `HighlightsStorage.js` | - | Все страницы с хайлайтами | ✅ **KEEP** | Менеджер localStorage |
| `DictionaryAPI.js` | - | Страницы с сохранением слов | ✅ **KEEP** | API обёртка |
| `Auth.js` | - | Header.js | ✅ **KEEP** | Telegram авторизация |
| `LoadingAnimation.js` | - | index.html, experimental.html, v3.html | ✅ **KEEP** | Анимация загрузки (после удаления experimental/v3 останется только в index.html) |

**Вердикт:** 8 компонентов остаются, 1 компонент удаляется.

---

### CSS Файлы

| Файл | Использование | Статус | Причина |
|------|---------------|--------|---------|
| `PatternCard.css` | PatternCard.js | ❌ **DELETE** | Стили удалённого компонента |

**Вердикт:** Единственный отдельный CSS файл удаляется (все остальные стили инлайн в HTML или в JS компонентах).

---

## 🔍 Детальный анализ компонентов

### ✅ HighlightCard.js - **KEEP** (Критический компонент)

**Файл:** `/static/components/HighlightCard.js`
**Размер:** 807 строк
**Зависимости от Highlight полей:**

```javascript
// Используемые поля Highlight:
✅ highlight              // KEEP - основное слово/фраза
✅ context                // KEEP - контекст использования
✅ highlight_translation  // KEEP - перевод слова/фразы
✅ dictionary_meanings    // KEEP - словарные значения
✅ type                   // KEEP (станет Enum WORD/EXPRESSION)

// НЕ использует удалённые поля:
❌ cefr_level            // НЕ используется ✅
❌ importance_score      // НЕ используется ✅
❌ why_interesting       // НЕ используется ✅
❌ pattern_template      // НЕ используется ✅
```

**Вердикт:** ✅ Компонент **НЕ затронут** удалением полей Highlight. Можно оставить без изменений.

**Функционал:**
- Отображает слово/фразу с переводом
- Показывает контекст с подсветкой слова
- Отображает словарные значения как теги
- Интерактивные кнопки (сохранить +, удалить -)
- Glass effect при hover/tap
- Адаптивный дизайн для мобильных

---

### ❌ PatternCard.js - **DELETE** (Legacy компонент)

**Файл:** `/static/components/PatternCard.js`
**Размер:** 70 строк
**Зависимости от Highlight полей:**

```javascript
// Используемые поля:
❌ pattern_template       // УДАЛЯЕТСЯ - шаблон конструкции
❌ why_interesting        // УДАЛЯЕТСЯ - объяснение
✅ context                // Остаётся
✅ highlight              // Остаётся
```

**Проблема:** Компонент полностью завязан на удаляемые поля `pattern_template` и `why_interesting`. Без них теряет смысл.

**Где используется:**
- `v3.html` - страница удаляется

**Вердикт:** ❌ Компонент удаляется вместе с v3.html.

---

### ✅ AnalysisForm.js - **KEEP** (Универсальная форма)

**Файл:** `/static/components/AnalysisForm.js`
**Размер:** 257 строк
**Функционал:**
- Универсальная форма для ввода текста
- Кнопка "WORDOORIO!"
- Обработка ошибок
- Адаптивный дизайн

**Где используется:**
- `index.html` ✅
- `experimental.html` ❌ (удаляется)

**Вердикт:** ✅ Остаётся, будет использоваться только в index.html.

---

### ✅ Header.js - **KEEP** (Унифицированная шапка)

**Файл:** `/static/components/Header.js`
**Функционал:**
- Логотип Wordoorio
- Навигация (Главная, Мои хайлайты, Словарь)
- Telegram авторизация
- Responsive меню для мобильных

**Вердикт:** ✅ Критический компонент, остаётся без изменений.

---

### ✅ HighlightsStorage.js - **KEEP** (LocalStorage менеджер)

**Файл:** `/static/js/HighlightsStorage.js`
**Функционал:**
- Сохранение хайлайтов в localStorage
- Управление сессиями анализа
- Дедупликация сохранённых слов
- API для получения всех сохранённых хайлайтов

**Зависимости от Highlight:**
```javascript
// Использует поля:
✅ highlight              // KEEP
✅ highlight_translation  // KEEP
✅ context                // KEEP
```

**Вердикт:** ✅ Не затронут удалением полей, остаётся без изменений.

---

### ✅ DictionaryAPI.js - **KEEP** (API обёртка)

**Файл:** `/static/js/DictionaryAPI.js`
**Функционал:**
- Обёртка над `/api/dictionary/*` endpoints
- Добавление слов в словарь
- Получение списка слов
- Получение статистики

**Вердикт:** ✅ Остаётся без изменений.

---

### ✅ DictionaryWordRow.js - **KEEP** (Компонент строки словаря)

**Файл:** `/static/components/DictionaryWordRow.js`
**Функционал:**
- Отображение слова в словаре
- Показ переводов и примеров
- Статистика использования

**Вердикт:** ✅ Остаётся без изменений.

---

## 🗑️ Что удалить на фронтенде

### 1. Удаляемые HTML страницы

#### experimental.html

**Путь:** `/templates/experimental.html`
**Размер:** ~607 строк
**Route:** `/experimental`
**Endpoint:** `/experimental/analyze` (POST)

**Почему удаляем:**
- Дублирует функционал index.html
- Использовала dual-prompt подход (v2_dual.py) - промпт удаляется
- Называется "EXPERIMENTAL" - явно временная страница
- После перехода на Yandex AI Studio агенты функционал объединяется в главной странице

**Что на ней было:**
- Форма анализа текста (как в index.html)
- Результаты разделены на табы: "Слова" / "Фразы" / "Все вместе"
- YouTube auto-fill логика (скопирована из index.html)
- Dual-prompt анализ (2 параллельных агента) - теперь это стандарт

**Зависимые компоненты (останутся работать в index.html):**
- AnalysisForm.js ✅
- HighlightCard.js ✅
- LoadingAnimation.js ✅

---

#### v3.html

**Путь:** `/templates/v3.html`
**Размер:** ~578 строк
**Route:** `/v3` (не существует в web_app.py!)
**Endpoint:** `/v3/analyze` (не существует в web_app.py!)

**Почему удаляем:**
- **Роут не существует** - мёртвый код
- Использовала v3_adaptive.py промпт (33KB!) - промпт удаляется
- Использовала **PatternCard.js** - компонент удаляется (зависит от удалённых полей)
- Показывала "языковые паттерны" - функционал убран из Highlight

**Что на ней было:**
- V3 adaptive prompt анализ
- Результаты разделены на табы: "Хайлайты" / "Паттерны" / "Все вместе"
- Логотип Wordoorio SVG (скопирован в другие страницы)
- PatternCard компоненты для отображения языковых конструкций

**Зависимые компоненты:**
- PatternCard.js ❌ (удаляется вместе со страницей)
- PatternCard.css ❌ (удаляется вместе со страницей)

---

### 2. Удаляемые JS компоненты

#### PatternCard.js

**Путь:** `/static/components/PatternCard.js`
**Размер:** 70 строк

**Проблема:**
```javascript
// Код компонента:
function createPatternCard(pattern, index) {
    return `
        <div class="pattern-template">
            <div class="template-text">${pattern.pattern_template}</div>  // ❌ УДАЛЕНО
        </div>
        <div class="pattern-explanation">
            <p>${pattern.why_interesting}</p>  // ❌ УДАЛЕНО
        </div>
    `;
}
```

**Зависимости:**
- `pattern.pattern_template` ❌ - поле удалено из Highlight
- `pattern.why_interesting` ❌ - поле удалено из Highlight

**Где используется:**
- `v3.html` ❌ (страница удаляется)

**Вердикт:** ❌ Компонент полностью зависит от удалённых полей. Удалить.

---

### 3. Удаляемые CSS файлы

#### PatternCard.css

**Путь:** `/static/components/PatternCard.css`
**Размер:** ~80 строк (оценка)

**Где используется:**
- `v3.html` ❌ (страница удаляется)

**Вердикт:** ❌ Стили для удалённого компонента. Удалить.

---

### 4. Удаляемые Routes (Backend)

#### `/experimental` (GET)

**Файл:** `web_app.py`
**Код:**
```python
@app.route('/experimental')
def experimental_page():
    return render_template('experimental.html')
```

**Вердикт:** ❌ Удалить вместе с experimental.html.

---

#### `/experimental/analyze` (POST)

**Файл:** `web_app.py`
**Код:** ~100 строк (вызывает dual-prompt анализ)

**Вердикт:** ❌ Удалить. Функционал переезжает в основной `/analyze` endpoint.

---

#### `/v3` (не существует)

**Вердикт:** ✅ Нечего удалять - роута не было.

---

#### `/v3/analyze` (не существует)

**Вердикт:** ✅ Нечего удалять - роута не было.

---

## ✅ Что остаётся на фронтенде

### Рабочие страницы (4 шт)

| Страница | Route | Назначение | Компоненты |
|----------|-------|------------|------------|
| **index.html** | `/` | Главная страница анализа | Header, AnalysisForm, HighlightCard, HighlightsStorage, DictionaryAPI |
| **my-highlights.html** | `/my-highlights` | Просмотр сохранённых хайлайтов | Header, HighlightCard, HighlightsStorage |
| **history.html** | `/history` | История анализов | - (нет общих компонентов) |
| **dictionary.html** | `/dictionary` | Словарь пользователя | Header, DictionaryWordRow, DictionaryAPI |

---

### Рабочие компоненты (8 шт)

| Компонент | Размер | Назначение | Статус |
|-----------|--------|------------|--------|
| `AnalysisForm.js` | 257 строк | Универсальная форма анализа | ✅ Без изменений |
| `HighlightCard.js` | 807 строк | Карточка слова/фразы | ✅ Без изменений |
| `Header.js` | ~200 строк | Универсальная шапка | ✅ Без изменений |
| `DictionaryWordRow.js` | ~100 строк | Строка словаря | ✅ Без изменений |
| `HighlightsStorage.js` | ~300 строк | LocalStorage менеджер | ✅ Без изменений |
| `DictionaryAPI.js` | ~150 строк | API обёртка | ✅ Без изменений |
| `Auth.js` | ~100 строк | Telegram авторизация | ✅ Без изменений |
| `LoadingAnimation.js` | ~100 строк | Анимация загрузки | ✅ Без изменений |

**Итого:** ~2014 строк рабочего кода остаётся.

---

## 📐 Архитектурные рекомендации

### 1. ✅ Компонентный подход - ХОРОШО

Текущая структура с переиспользуемыми компонентами отличная:

```
Компоненты как строительные блоки:
- Header.js - используется на всех страницах
- AnalysisForm.js - универсальная форма
- HighlightCard.js - единообразное отображение результатов
```

**Рекомендация:** Сохранить текущий подход.

---

### 2. ✅ Разделение ответственности - ХОРОШО

Каждый компонент отвечает за одну задачу:

```
- HighlightsStorage.js - ТОЛЬКО работа с localStorage
- DictionaryAPI.js - ТОЛЬКО HTTP запросы к API
- Auth.js - ТОЛЬКО Telegram авторизация
```

**Рекомендация:** Продолжать следовать Single Responsibility Principle.

---

### 3. ✅ Инлайн стили в компонентах - ПРИЕМЛЕМО

Каждый JS компонент содержит свои стили:

```javascript
function getHighlightCardStyles() {
    return `
        .highlight-card-v6 { ... }
        .highlight-title { ... }
    `;
}
```

**Преимущества:**
- Нет конфликтов имён CSS классов
- Компонент самодостаточный (не нужен отдельный CSS)
- Легко удалить компонент вместе со стилями

**Недостатки:**
- Невозможно переиспользовать стили между компонентами
- Дублирование общих стилей (цвета, отступы)

**Рекомендация:**
- ✅ Для уникальных стилей компонента - оставить инлайн
- ⚠️  Для общих стилей (colors, spacing) - рассмотреть CSS переменные:

```css
/* Пример: общие переменные в index.html */
:root {
    --color-primary: #4CAF50;
    --spacing-md: 16px;
    --radius-md: 12px;
}
```

Уже используется в `experimental.html` - можно перенести в глобальный `<style>` в `<head>` всех страниц.

---

### 4. ✅ LocalStorage для offline работы - ХОРОШО

`HighlightsStorage.js` позволяет сохранять слова локально, даже без авторизации:

```javascript
// Пользователь может:
1. Сохранять слова локально
2. Просматривать их в /my-highlights
3. Синхронизировать с сервером при авторизации
```

**Рекомендация:** Отличный UX pattern. Сохранить.

---

### 5. ⚠️ Дублирование кода между страницами

**Проблема:** Общий код повторяется в каждой странице:

```html
<!-- index.html, experimental.html, v3.html - одинаковый код: -->
<style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:...');
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Inter', sans-serif; ... }
</style>
```

**Решение (НЕ критично):**
- Можно вынести в `/static/css/global.css`
- Но т.к. страниц всего 4, дублирование приемлемо

**Рекомендация:**
- ✅ Оставить как есть (не критично)
- 🔮 Будущая оптимизация: создать `/static/css/global.css` и подключать `<link>`

---

### 6. ✅ Адаптивность - ХОРОШО

Все компоненты имеют responsive дизайн:

```css
/* HighlightCard.js */
@media (max-width: 768px) { ... }  /* Планшеты */
@media (max-width: 480px) { ... }  /* Мобильные */
```

**Рекомендация:** Отличная практика. Сохранить.

---

## 🎯 План действий: Фронтенд чистка

### Этап 1: Удаление файлов

```bash
# Удалить HTML страницы
rm /templates/experimental.html
rm /templates/v3.html

# Удалить JS компоненты
rm /static/components/PatternCard.js

# Удалить CSS файлы
rm /static/components/PatternCard.css
```

---

### Этап 2: Удаление routes в web_app.py

**Файл:** `/web_app.py`

```python
# УДАЛИТЬ:

@app.route('/experimental')
def experimental_page():
    return render_template('experimental.html')

@app.route('/experimental/analyze', methods=['POST'])
def experimental_analyze():
    # ~100 строк кода
    ...
```

---

### Этап 3: Проверка ссылок

**Проверить:**
- ❌ Убедиться что нет ссылок на `/experimental` в Header.js или других компонентах
- ❌ Убедиться что нет ссылок на `/v3` в коде

**Команда для проверки:**
```bash
grep -r "experimental" /static/
grep -r "/v3" /static/
grep -r "PatternCard" /static/
```

---

### Этап 4: Объединение функционала

**Задача:** Dual-prompt функционал из experimental.html перенести в index.html.

**Изменения в index.html:**

```diff
<!-- До: -->
<div id="results" class="results">
    <h2>📊 Результаты анализа</h2>
    <div id="highlightsList"></div>
</div>

<!-- После: (опционально - добавить табы как в experimental) -->
<div id="results" class="results">
    <h2>📊 Результаты анализа</h2>
    <div class="results-tabs">
        <button class="tab-button active" onclick="showTab('words')">Слова</button>
        <button class="tab-button" onclick="showTab('phrases')">Фразы</button>
        <button class="tab-button" onclick="showTab('all')">Все вместе</button>
    </div>
    <div id="wordsTab" class="tab-content active"></div>
    <div id="phrasesTab" class="tab-content"></div>
    <div id="allTab" class="tab-content"></div>
</div>
```

**Вопрос к пользователю:** Нужны ли табы "Слова/Фразы/Все" на главной странице? Или оставить как есть (единый список)?

---

### Этап 5: Тестирование

**Проверить работоспособность:**

1. ✅ **index.html** (`/`)
   - Форма анализа работает
   - Результаты отображаются
   - Кнопки сохранения/удаления работают

2. ✅ **my-highlights.html** (`/my-highlights`)
   - Список сохранённых слов показывается
   - Фильтры (Слова/Фразы) работают

3. ✅ **history.html** (`/history`)
   - История анализов показывается
   - Поиск работает

4. ✅ **dictionary.html** (`/dictionary`)
   - Словарь показывается
   - Фильтры работают

---

## 📊 Метрики чистки

### До чистки

```
HTML страницы:     6 файлов  (~3500 строк)
JS компоненты:     9 файлов  (~2200 строк)
CSS файлы:         1 файл    (~80 строк)
Backend routes:    ~600 строк (с experimental/v3 endpoints)

Итого: ~6380 строк фронтенд кода
```

### После чистки

```
HTML страницы:     4 файла   (~2300 строк) ↓ -34%
JS компоненты:     8 файлов  (~2014 строк) ↓ -8%
CSS файлы:         0 файлов  (0 строк)     ↓ -100%
Backend routes:    ~400 строк              ↓ -33%

Итого: ~4714 строк фронтенд кода ↓ -26%
```

**Результат:** Уменьшение кодовой базы фронтенда на **26%** (~1666 строк).

---

## ✅ Чек-лист выполнения

### Файлы для удаления

- [ ] `/templates/experimental.html`
- [ ] `/templates/v3.html`
- [ ] `/static/components/PatternCard.js`
- [ ] `/static/components/PatternCard.css`

### Routes для удаления (web_app.py)

- [ ] `@app.route('/experimental')` → `def experimental_page()`
- [ ] `@app.route('/experimental/analyze')` → `def experimental_analyze()`

### Проверки

- [ ] Нет ссылок на `/experimental` в коде
- [ ] Нет ссылок на `/v3` в коде
- [ ] Нет импортов `PatternCard` в коде
- [ ] Все 4 рабочие страницы открываются
- [ ] HighlightCard.js работает на всех страницах
- [ ] Сохранение/удаление хайлайтов работает

### Опциональные улучшения (не критично)

- [ ] Добавить табы "Слова/Фразы/Все" в index.html (по желанию пользователя)
- [ ] Вынести общие CSS переменные в global.css (не критично)

---

## 🎯 Критерии успеха

### Минимальные требования

✅ **Удалены все legacy страницы** (experimental.html, v3.html)
✅ **Удалён PatternCard компонент** (и CSS)
✅ **Удалены experimental routes** в web_app.py
✅ **Все 4 рабочие страницы работают** без ошибок
✅ **HighlightCard.js работает** на всех страницах

### Дополнительно (желательно)

🔮 Функционал из experimental.html перенесён в index.html (табы Слова/Фразы)
🔮 Нет мёртвых ссылок на удалённые страницы
🔮 Нет console.error в браузере

---

## 📝 Рекомендации на будущее

### 1. CSS переменные для общих стилей

**Сейчас:** Каждая страница дублирует цвета, отступы, радиусы.

**Решение:** Создать `/static/css/variables.css`:

```css
:root {
    /* Цвета */
    --color-primary: #4CAF50;
    --color-bg: linear-gradient(90deg, #39A0B3 0%, #1B7A94 100%);

    /* Отступы */
    --spacing-xs: 8px;
    --spacing-md: 16px;
    --spacing-lg: 24px;

    /* Радиусы */
    --radius-sm: 6px;
    --radius-md: 12px;
    --radius-lg: 16px;
}
```

Подключать в каждом HTML:
```html
<link rel="stylesheet" href="/static/css/variables.css">
```

**Приоритет:** Низкий (не критично для текущей задачи).

---

### 2. JS модули (ES6 modules)

**Сейчас:** Все компоненты загружаются через `<script src="...">` теги.

**Проблема:** Глобальное пространство имён, сложно отслеживать зависимости.

**Решение:** Перейти на ES6 модули:

```javascript
// HighlightCard.js
export function createHighlightCard(...) { ... }

// index.html
<script type="module">
    import { createHighlightCard } from '/static/components/HighlightCard.js';
</script>
```

**Приоритет:** Средний (упростит масштабирование в будущем).

---

### 3. Бандлинг (Webpack/Vite)

**Сейчас:** Все JS/CSS загружаются отдельными файлами.

**Проблема:** Много HTTP запросов, медленная загрузка.

**Решение:** Использовать Vite или Webpack для бандлинга:

```bash
# Вместо:
<script src="/static/components/Header.js"></script>
<script src="/static/components/AnalysisForm.js"></script>
<script src="/static/components/HighlightCard.js"></script>
...

# Один файл:
<script src="/static/bundle.js"></script>
```

**Приоритет:** Низкий (оптимизация производительности для больших проектов).

---

### 4. TypeScript

**Сейчас:** Vanilla JavaScript, нет типизации.

**Проблема:** Сложно отловить ошибки на этапе разработки.

**Решение:** Перейти на TypeScript:

```typescript
interface Highlight {
    highlight: string;
    context: string;
    highlight_translation: string;
    dictionary_meanings: string[];
    type: 'word' | 'expression';
}

function createHighlightCard(highlight: Highlight, index: number): string {
    // TypeScript проверит что все поля есть
}
```

**Приоритет:** Средний (для серьёзных проектов).

---

## 🏁 Заключение

### Итоговый вердикт фронтенда

✅ **Архитектура:** Чистая, компонентная, хорошо структурированная
✅ **Код:** Читаемый, переиспользуемый, адаптивный
✅ **Проблема:** Только legacy код (experimental/v3 страницы)

### План действий

1. ❌ **Удалить:** experimental.html, v3.html, PatternCard.js/css
2. ❌ **Удалить routes:** /experimental, /experimental/analyze
3. ✅ **Проверить:** Все 4 рабочие страницы работают
4. 🔮 **Опционально:** Перенести табы из experimental.html в index.html

### Результат

- **-26% кода** (1666 строк удалено)
- **100% работоспособности** (все рабочие страницы остаются)
- **0 breaking changes** (HighlightCard.js не затронут)

---

**Статус:** ✅ Готово к реализации
**Блокеры:** Нет
**Риски:** Минимальные (удаляем только legacy код)

---

## 📞 Вопросы к пользователю

1. **Табы на главной странице:**
   Нужны ли табы "Слова" / "Фразы" / "Все вместе" на index.html?
   Или оставить единый список всех хайлайтов?

2. **CSS переменные:**
   Вынести общие стили в global.css?
   Или оставить инлайн (текущий подход)?

3. **Дополнительные улучшения:**
   Есть ли другие улучшения фронтенда которые вы хотели бы сделать в рамках этой задачи?

---

*Документ подготовлен для максимальной чистки фронтенда в рамках рефакторинга агентов Wordoorio.*
