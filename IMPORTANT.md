# 🔒 АКТУАЛЬНАЯ КОНФИГУРАЦИЯ WORDOORIO

## 🚀 ТЕКУЩИЙ ДЕПЛОЙ
- **Архитектура:** VM на Yandex Compute Cloud
- **Основной сайт:** https://wordoorio.ru
- **Experimental версия:** https://wordoorio.ru/experimental
- **Статус:** ✅ Продакшн работает

## 🔧 УПРАВЛЕНИЕ ПРОЕКТОМ

### Основные команды деплоя:
```bash
# Обновление experimental версии
./deploy_experimental.sh

# Исправление таймаутов (если нужно)
./fix_timeout_final.sh

# Диагностика через API
./deploy_via_api.sh
```

### Проверка состояния:
```bash
# SSH подключение к серверу
ssh yc-user@158.160.126.200

# Проверка сервиса
sudo systemctl status wordoorio

# Просмотр логов
sudo journalctl -u wordoorio -f
```

## 🔑 YANDEX CLOUD КОНФИГУРАЦИЯ

### Основные ID:
- **Folder ID:** b1gcdpfvt5vkfn3o9nm1
- **Registry ID:** crp1mj4p9ro0clhe5t61 (для Docker, если понадобится)

### Сервер:
- **IP:** 158.160.126.200
- **User:** yc-user
- **Домен:** wordoorio.ru

## ⚙️ АВТОМАТИЗАЦИЯ

### Автообновление токенов:
- **Cron:** каждые 3 часа (0 */3 * * *)
- **Скрипт:** `/var/www/wordoorio/server_token_refresh.py`
- **Логи:** `/var/log/wordoorio-tokens.log`

### Мониторинг токенов:
```bash
# Проверка cron задачи
ssh yc-user@158.160.126.200 "crontab -l"

# Просмотр логов автообновления
ssh yc-user@158.160.126.200 "sudo tail -f /var/log/wordoorio-tokens.log"

# Получение нового токена вручную
~/yandex-cloud/bin/yc iam create-token
```

## 📁 АКТУАЛЬНАЯ СТРУКТУРА ПРОЕКТА

```
wordoorio/
├── web_app.py                    # Основное Flask приложение  
├── agents/
│   ├── agent_1.py               # YouTube агент (FUTURE)
│   └── agent_2.py               # AI анализ лексики (АКТИВЕН)
├── core/
│   ├── yandex_ai_client.py      # Основной AI клиент
│   └── experimental_ai_client.py # Dual-prompt клиент
├── templates/
│   ├── index.html               # Главная страница
│   ├── experimental.html        # Experimental версия
│   └── history.html             # История анализов
├── refresh_token.py             # Локальное автообновление
├── server_token_refresh.py      # Продакшн автообновление
└── deploy_experimental.sh       # Деплой experimental версии
```

## 🎯 AI ФУНКЦИОНАЛЬНОСТЬ

### Основная версия:
- **URL:** https://wordoorio.ru
- **AI клиент:** `core/yandex_ai_client.py`  
- **Агент:** `agents/agent_2.py`
- **Промпт:** Единый анализ выразительной лексики

### Experimental версия (Dual-prompt):
- **URL:** https://wordoorio.ru/experimental
- **AI клиент:** `core/experimental_ai_client.py`
- **Особенность:** Два отдельных промпта для слов и фраз
- **Таймауты:** Увеличены до 180 секунд

### API эндпоинты:
- `GET /` - Главная страница
- `POST /analyze` - Основной анализ
- `GET /experimental` - Experimental страница  
- `POST /experimental/analyze` - Dual-prompt анализ

## 🐛 ТРАБЛШУТИНГ

### Проблемы с токенами
```
Для AI анализа нужны токены Yandex GPT
```
**Решение:** Токены должны автообновляться каждые 3 часа. Проверить:
```bash
ssh yc-user@158.160.126.200 "sudo tail -f /var/log/wordoorio-tokens.log"
```

### Таймауты на experimental
```
Ошибка соединения с сервером
```
**Решение:** Увеличить таймауты (уже исправлено):
```bash
./fix_timeout_final.sh
```

### Сервис не работает
```bash
# Проверить статус сервиса
ssh yc-user@158.160.126.200 "sudo systemctl status wordoorio"

# Перезапустить сервис
ssh yc-user@158.160.126.200 "sudo systemctl restart wordoorio"

# Просмотр логов
ssh yc-user@158.160.126.200 "sudo journalctl -u wordoorio -f"
```

## 💰 ТЕКУЩИЕ РАСХОДЫ
- **VM (1 vCPU, 1GB RAM):** ~567₽/месяц
- **Домен wordoorio.ru:** Уже оплачен
- **Yandex GPT API:** По факту использования

## 🔄 АКТУАЛЬНЫЙ WORKFLOW

1. **Локальная разработка:** `python web_app.py` (порт 8081)
2. **Тестирование experimental:** http://localhost:8081/experimental
3. **Деплой изменений:** `./deploy_experimental.sh`
4. **Проверка продакшна:** https://wordoorio.ru/experimental

---
**Последнее обновление:** 30 ноября 2025  
**Версия:** VM-based deployment с experimental dual-prompt v3.0