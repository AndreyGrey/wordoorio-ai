# 🏗️ АРХИТЕКТУРА СИСТЕМЫ WORDOORIO AI v3.0

## 🎯 ОБЩЕЕ ОПИСАНИЕ СИСТЕМЫ

Wordoorio AI - это веб-приложение для анализа английской лексики с **dual-prompt архитектурой**. Система помогает изучающим язык находить "живые", выразительные слова и выражения в тексте через:

- **Основную версию** (https://wordoorio.ru) - стабильный единый промпт
- **Experimental версию** (https://wordoorio.ru/experimental) - инновационный dual-prompt анализ

Обе версии используют искусственный интеллект Yandex GPT, но experimental версия применяет два специализированных промпта для максимального качества анализа.

---

## 📊 АРХИТЕКТУРНАЯ СХЕМА (v3.0)

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────────┐
│   Пользователь  │───▶│   Веб-интерфейс  │───▶│    Flask Backend    │
│    (Браузер)    │    │ index.html       │    │    (web_app.py)     │
│                 │    │ experimental.html│    │                     │
└─────────────────┘    └──────────────────┘    └─────────────────────┘
                                                           │
                                                           ▼
                       ┌─────────────────────────────────────────────────┐
                       │              АГЕНТНАЯ СИСТЕМА                   │
                       │  ┌─────────────┐         ┌─────────────────┐   │
                       │  │   Agent 1   │         │    Agent 2      │   │
                       │  │  (YouTube)  │         │ (AI Vocabulary) │   │
                       │  │ [FUTURE]    │         │   [АКТИВЕН]     │   │
                       │  └─────────────┘         └─────────────────┘   │
                       └─────────────────────────────────────────────────┘
                                                           │
                                                           ▼
                       ┌─────────────────────────────────────────────────┐
                       │               YANDEX AI CORE                    │
                       │                                                 │
                       │  ┌─────────────────────┐  ┌─────────────────┐  │
                       │  │ ОСНОВНАЯ ВЕРСИЯ     │  │ EXPERIMENTAL    │  │
                       │  │ yandex_ai_client.py │  │ experimental_   │  │
                       │  │ Единый промпт GPT   │  │ ai_client.py    │  │
                       │  │                     │  │ DUAL ПРОМПТЫ:   │  │
                       │  │ ┌─────────────────┐ │  │ ┌─────────────┐ │  │
                       │  │ │ Yandex GPT      │ │  │ │ GPT Слова   │ │  │
                       │  │ │ (анализ)        │ │  │ │ GPT Фразы   │ │  │
                       │  │ └─────────────────┘ │  │ └─────────────┘ │  │
                       │  └─────────────────────┘  └─────────────────┘  │
                       │                                                 │
                       │  ┌─────────────────────────────────────────┐   │
                       │  │     Dictionary API + Yandex Translate  │   │
                       │  └─────────────────────────────────────────┘   │
                       └─────────────────────────────────────────────────┘
```

---

## 🧩 КОМПОНЕНТЫ СИСТЕМЫ

### 1. **ФРОНТЕНД (Пользовательский интерфейс)**

**Файл:** `templates/index.html`

**Назначение:** Современный веб-интерфейс для ввода текста и отображения результатов анализа.

**Ключевые особенности:**
- 🎨 **Современный дизайн** - градиенты, тени, анимации
- 📱 **Отзывчивость** - работает на всех устройствах  
- 🔄 **Умная анимация загрузки** - показывает реальные слова из пользовательского текста
- 🎯 **Встроенные ресурсы** - логотип и favicon встроены в HTML
- ✨ **Интерактивность** - подсветка найденных слов в тексте

**Технологии:**
- HTML5 + CSS3 (Flexbox, Grid)
- Vanilla JavaScript (ES6+)
- Google Fonts (Inter, Poppins)
- SVG графика

---

### 2. **БЭКЕНД (Веб-сервер)**

**Файл:** `web_app.py`

**Назначение:** Flask веб-сервер, который обрабатывает HTTP запросы и координирует работу агентов.

**Архитектура:**
```python
Flask App (web_app.py)
├── GET /          → Главная страница (index.html)
├── POST /analyze  → API для анализа текста
└── Middleware     → Обработка ошибок, CORS
```

**Логика работы:**
1. Принимает POST запрос с текстом
2. Валидирует входные данные (минимум 5 слов)
3. Передает текст в Agent 2 для AI анализа
4. Возвращает JSON с результатами или ошибкой

**Обработка ошибок:**
- Пустой текст → "Текст не может быть пустым"
- Короткий текст → "Текст слишком короткий (минимум 5 слов)"  
- Нет токенов → "Для AI анализа нужны токены Yandex GPT"
- Критические ошибки → Подробное логирование

---

### 3. **АГЕНТНАЯ СИСТЕМА**

#### **Agent 1 (YouTube Processor)** 
**Файл:** `agents/agent_1.py`

**Статус:** 🔮 **FUTURE FEATURE** - не используется в текущей версии

**Назначение:** Извлечение субтитров и контента из YouTube видео для последующего анализа.

**Планируемая функциональность:**
- Извлечение Video ID из различных форматов YouTube URL
- Получение метаданных видео через YouTube API
- Скачивание английских субтитров
- Очистка и подготовка текста для анализа
- Объединение заголовка, описания и субтитров

**Зависимости:** 
- YouTube API ключ
- youtube-transcript-api библиотека

#### **Agent 2 (AI Vocabulary Analyzer)**
**Файл:** `agents/agent_2.py`

**Статус:** ✅ **АКТИВЕН** - ядро системы

**Назначение:** Интеллектуальный анализ текста через Yandex GPT для поиска выразительной лексики.

**Архитектура:**
```python
AIVocabularyAnalyzer
├── analyze_text()           → Главный метод анализа
├── _calculate_text_stats()  → Базовая статистика текста
├── _extract_ai_highlights() → AI анализ через Yandex GPT
└── _assess_difficulty()     → Оценка сложности текста
```

**Принципы работы:**
- **ТОЛЬКО AI анализ** - никаких примитивных алгоритмов
- **Fallback система** - если GPT недоступен, возвращает ошибку
- **Контекстная валидация** - проверяет что найденные слова есть в тексте
- **Статистический анализ** - подсчет метрик для оценки сложности

---

### 4. **YANDEX AI CORE (Ядро ИИ)**

**Файл:** `core/yandex_ai_client.py`

**Назначение:** Интеграция с облачными AI сервисами Yandex для анализа лексики.

#### **Класс LinguisticHighlight**
```python
@dataclass
class LinguisticHighlight:
    highlight: str              # Найденное слово/фраза
    context: str               # Предложение с контекстом
    context_translation: str   # Перевод слова/фразы
    english_example: str       # Пример использования
    russian_example: str       # Русский пример
    cefr_level: str           # Уровень сложности (A1-C2)
    importance_score: int      # Важность (0-100)
    dictionary_meanings: []    # Словарные значения
    why_interesting: str       # Объяснение важности
```

#### **Класс YandexAIClient**
**Основные методы:**

**`analyze_linguistic_highlights(text)`**
- Создает умный промпт для Yandex GPT
- Определяет количество хайлайтов по длине текста (3-30)
- Отправляет запрос к Yandex GPT API
- Парсит JSON ответ от нейросети
- Валидирует найденные слова в контексте
- Добавляет переводы через Yandex Translate

**`_create_highlights_prompt(text, target_count)`**
- Создает детальный промпт для GPT с четкими инструкциями
- Определяет что БРАТЬ (выразительные слова, коллокации, идиомы)
- Определяет что НЕ БРАТЬ (техническая терминология, базовая лексика)
- Требует JSON формат ответа

**`_get_dictionary_meanings(word)`**
- Интегрируется с Free Dictionary API
- Получает английские определения слов
- Переводит определения через Yandex Translate
- Обрабатывает только отдельные слова (не фразы)

**Система безопасности:**
- Все токены хранятся в переменных окружения
- Graceful degradation при недоступности API
- Подробное логирование ошибок
- Таймауты для всех внешних запросов

---

## 🔄 ПОТОК ДАННЫХ

### **Пользовательский сценарий:**
```
1. Пользователь вводит английский текст в веб-форму
                        ↓
2. JavaScript отправляет POST /analyze с текстом
                        ↓
3. Flask (web_app.py) валидирует данные
                        ↓
4. Вызывается Agent 2 → AIVocabularyAnalyzer
                        ↓
5. Agent 2 вызывает YandexAIClient → Yandex GPT
                        ↓
6. GPT анализирует текст и возвращает JSON хайлайтов
                        ↓
7. Система добавляет переводы через Yandex Translate
                        ↓
8. Результат упаковывается в JSON ответ
                        ↓
9. Фронтенд получает ответ и отображает результаты
                        ↓
10. Пользователь видит выделенные слова с переводами
```

---

## 🛡️ БЕЗОПАСНОСТЬ И НАДЕЖНОСТЬ

### **Защита данных:**
- Все API токены в `.env` файле (не в коде!)
- `.env` исключен из git через `.gitignore`
- Валидация всех пользовательских входных данных
- Защита от XSS через правильное экранирование

### **Обработка ошибок:**
- Graceful degradation при недоступности Yandex API
- Детальные сообщения об ошибках для разработчика
- Понятные сообщения для пользователя
- Логирование всех критических ошибок

### **Производительность:**
- Асинхронные запросы к внешним API
- Таймауты для предотвращения зависаний
- Оптимизированные промпты для GPT
- Кэширование переводов (в планах)

---

## 🚀 РАЗВЕРТЫВАНИЕ (реализовано)

### **Текущее (VM Production):**
- ✅ Виртуальная машина Yandex Compute Cloud
- ✅ Ubuntu 22.04 + nginx + Flask + gunicorn + systemd
- ✅ SSL сертификат Let's Encrypt с автообновлением
- ✅ Автообновление токенов каждые 3 часа через cron
- ✅ Домен wordoorio.ru с experimental версией
- **Стоимость:** 567₽/месяц (2 vCPU 50%, 1GB RAM, 10GB SSD)

### **Устаревшее (Cloud Functions):**
- ❌ Serverless функции (не используется)
- ❌ Ограничения размера архива
- ❌ Cold start проблемы

---

## 📈 МАСШТАБИРОВАНИЕ

### **Горизонтальное:**
- Добавление новых агентов (YouTube, PDF, etc.)
- Микросервисная архитектура
- Load Balancer для распределения нагрузки

### **Вертикальное:**
- Увеличение ресурсов VM через консоль
- 1GB → 2GB → 4GB RAM по мере роста
- Добавление SSD дисков для производительности

### **Оптимизация:**
- Redis для кэширования переводов
- PostgreSQL для хранения истории анализов
- CDN для статических файлов

---

## 🧪 EXPERIMENTAL СИСТЕМА (новая)

### **Dual-Prompt Architecture (v3.0)**
```
EXPERIMENTAL ВЕРСИЯ
├── URL: /experimental  
├── API: /experimental/analyze
├── AI клиент: core/experimental_ai_client.py
└── Особенность: Два параллельных промпта к Yandex GPT
```

#### **Принципы dual-prompt анализа:**
1. **Промпт #1: Выразительные слова** - фокус на отдельные слова
2. **Промпт #2: Фразы и коллокации** - фокус на словосочетания
3. **Параллельное выполнение** - два независимых запроса к GPT
4. **Раздельная обработка** - разные алгоритмы для слов и фраз
5. **Объединенный результат** - структура `{words: [...], phrases: [...]}`

#### **Технические улучшения:**
- **Таймауты:** Увеличены до 180 секунд (vs 30 в основной версии)
- **Переводы:** Исправлена логика - переводится только хайлайт, не весь контекст
- **Валидация:** Строгая проверка наличия хайлайтов в тексте
- **UI:** Табы для раздельного просмотра слов и фраз

#### **Сравнение версий:**
| Характеристика | Основная версия | Experimental |
|---|---|---|
| Промптов к GPT | 1 | 2 |
| Время анализа | ~10-30 сек | ~60-120 сек |
| Точность | Хорошая | Повышенная |
| Разделение слов/фраз | Смешанное | Раздельное |
| Интерфейс | Единый список | Табы (Слова/Фразы/Все) |

---

## 🎯 БУДУЩЕЕ РАЗВИТИЕ

### **Agent 1 (YouTube)** [ПЛАНЫ]:
- Интеграция с YouTube API
- Анализ субтитров популярных каналов
- Подборки слов по темам

### **Experimental → Production:**
- Тестирование dual-prompt подхода
- Сравнение качества результатов
- Возможный переход на dual-prompt как основной

### **Дополнительные источники:**
- PDF документы
- Веб-страницы  
- Подкасты (транскрипция)
- Электронные книги

### **Умные функции:**
- Персональные словари пользователей
- Система повторений (spaced repetition)
- AI-generated упражнения
- Интеграция с Anki

### **Аналитика:**
- Статистика изучения слов
- Прогресс пользователя
- Рекомендации материалов

---

## 📊 ТЕКУЩАЯ АРХИТЕКТУРА (v3.0)

**Основная версия:** https://wordoorio.ru (стабильная)  
**Experimental:** https://wordoorio.ru/experimental (тестирование)

**Развертывание:** VM на Yandex Compute Cloud  
**Автоматизация:** Токены автообновляются каждые 3 часа  
**Мониторинг:** Логи, systemd, nginx  

---

**💡 Ключевая особенность:** Система эволюционирует от единого промпта к dual-prompt архитектуре для максимального качества анализа через искусственный интеллект.